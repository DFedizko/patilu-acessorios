# RF-03 — Base de documentação/validação (schemas, OpenAPI, Scalar)

> Subtask autocontida. Implemente lendo só este arquivo + as seções citadas da TECH_SPEC.

| | |
| --- | --- |
| **Tipo** | back-end (doc/validação) |
| **Onda** | 0 |
| **Depende de** | RF-01 (deve estar `DONE` em `progress-claude.txt`) |
| **Habilita** | RF-06, RF-07, RF-10, RF-12 (todo endpoint novo adiciona schema + registra caminho) |
| **Hotspots tocados** | `src/lib/schemas.ts`, `src/lib/openapi.ts`, `src/app/api/openapi.json/route.ts`, `src/app/api/reference/route.ts` |

## 1. Objetivo (o que este RF entrega)

**Negócio:** dá à equipe (e a cada agente seguinte) uma documentação de API navegável e uma fonte única de validação, de modo que cada endpoint criado depois já nasça validado e documentado.

**Técnico:** estabelece o `src/lib/schemas.ts` com o `periodQuerySchema` compartilhado e a **convenção de DTO via `z.input`**, o documento `src/lib/openapi.ts` (zod-openapi) e as rotas `/api/openapi.json` e `/api/reference` (Scalar) servindo um documento válido (vazio por enquanto).

## 2. Contexto (por quê)

**Zod é a fonte da verdade** de validação e de documentação (TECH_SPEC §15). Todos os endpoints de leitura por período (Pedidos, Histórico, Dashboard, Ads) reutilizam um único `periodQuerySchema` (§2), e cada caso de uso reaproveita o tipo inferido do schema do seu endpoint (`z.input`) como `Input` (§6.1) — assim validação e tipagem nunca divergem. Este RF entrega a base dessa convenção e a UI de doc; os RFs de cada domínio depois **estendem** estes arquivos (de forma aditiva).

## 3. Início — pré-condições (o que já precisa existir)

- **RF-01 `DONE`** (VOs/erros/DI prontos — embora este RF não dependa deles em runtime, faz parte da Onda 0 sequencial).
- Arquivos já presentes no repo a **estender** (não recriar do zero):
  - `src/lib/schemas.ts` — hoje tem `looseItemSchema`, `newCategorySchema`, `tierSchema` (legado do front com seed). **Adicione** os novos schemas/DTOs sem quebrar o existente.
  - `src/lib/openapi.ts` — `getOpenApiDocument()` com `paths: {}`.
  - `src/app/api/openapi.json/route.ts` e `src/app/api/reference/route.ts` — já existem; ligar a um documento válido servido.
- `zod-openapi` e Scalar já disponíveis no projeto (stack do CLAUDE.md).

## 4. Escopo — entregáveis (faça exatamente isto)

1. **`src/lib/schemas.ts`** (estender):
   - Adicionar `periodQuerySchema` (§2) — `?period=today|week|month|custom` + `from`/`to` para `custom`, com `refine` exigindo `from`+`to` quando `custom`.
   - Documentar/estabelecer a **convenção de DTO**: cada schema de entrada de endpoint exporta também `export type <Action>DTO = z.input<typeof <action>Schema>;` consumido pelo contrato do use case (§6.1/§8). Exemplos de referência (`createTierSchema`/`CreateTierDTO`, `savePackingSchema`/`SavePackingDTO`) ficam documentados como padrão; **a criação dos schemas concretos de cada domínio é dos RFs seguintes** — aqui entra apenas `periodQuerySchema` + a convenção.
2. **`src/lib/openapi.ts`** (estender) — manter `getOpenApiDocument()` retornando um documento **válido** (info/servers/paths), pronto para receber caminhos aditivos. Documentar também as respostas de erro (códigos §9) quando os caminhos forem adicionados pelos RFs seguintes.
3. **`src/app/api/openapi.json/route.ts`** — servir o documento de `getOpenApiDocument()` como JSON.
4. **`src/app/api/reference/route.ts`** — servir a UI **Scalar** apontando para `/api/openapi.json`.

### Fora do escopo (NÃO faça)

- Criar schemas concretos de domínio (`createTierSchema`, `savePackingSchema`, etc.) e registrar seus caminhos — isso é de RF-06/RF-07/RF-10/RF-12/RF-14/RF-16/RF-17/RF-18 (cada um adiciona o seu, aditivo).
- Qualquer caso de uso, rota de API de domínio ou gateway.
- `HttpClient`/`ApiError`/erros do front (RF-02).
- Testes automatizados (não é alvo — §14).

## 5. Fim — Definition of Done

- `GET /api/reference` abre a UI Scalar com um **documento válido** (vazio de caminhos é aceitável).
- `GET /api/openapi.json` devolve o documento JSON de `getOpenApiDocument()`.
- `periodQuerySchema` exportado de `src/lib/schemas.ts`, com a regra de `custom` exigindo `from`+`to`.
- Convenção de DTO (`z.input`) documentada e pronta para os RFs seguintes reusarem; schemas legados do front não quebram.
- `bun run lint` limpo; sem `any`; import alias `@/*`; sem `default export` (exceto handlers `route.ts` do Next); 4 espaços, `printWidth` 120.

## 6. Testes que cobrem este RF (§14.2)

**Não há testes automatizados.** Doc/validação base não é alvo de teste (§14). Verificação: `/api/reference` abre; `periodQuerySchema` exportado; lint/tipo limpos.

## 7. Trecho da PRD que fala deste RF

**Seletor de período** reutilizado em Pedidos, Histórico e Dashboard (PRD §5):

> - **F2a** — Lista os pedidos do TikTok, com **seletor de período** reaproveitando a lógica do Dashboard (hoje / semana / mês / personalizado).
> - **F3** — Seleção de período (hoje / semana / mês / personalizado) — mesma lógica do Dashboard.
> - **F4 / RN-4.3** — O período "personalizado" aceita data inicial e final escolhidas pelo usuário.

## 8. Trecho da TECH_SPEC que fala deste RF

**Period query scheme (§2)** — reutilizado em todos os endpoints de leitura por período (`?period=today|week|month|custom` e, para `custom`, `&from=YYYY-MM-DD&to=YYYY-MM-DD`):

```ts
// @/lib/period.ts  — schema compartilhado
export const periodQuerySchema = z
    .object({
        period: z.enum(["today", "week", "month", "custom"]),
        from: z.iso.date().optional(),
        to: z.iso.date().optional(),
    })
    .refine((v) => v.period !== "custom" || (v.from && v.to), {
        message: "Período personalizado exige 'from' e 'to'",
        path: ["from"],
    });
```

> Nota: a spec ilustra o schema em `@/lib/period.ts`, mas o repo centraliza schemas em `src/lib/schemas.ts` (arquivo já existente) — adicione `periodQuerySchema` lá, mantendo a convenção de um único módulo de schemas.

**Schemas Zod centrais e convenção de DTO (§8):**

> **Convenção:** todo schema de entrada de endpoint **exporta também um DTO** via `z.input<typeof schema>`, consumido pelo contrato do caso de uso correspondente (§6.1). Assim o `Input` do use case e a validação do endpoint são a mesma coisa.

```ts
export const createTierSchema = z.object({
    name: z.string().trim().min(1, "Informe o nome da faixa"),
    costReais: z.number().positive("Informe um custo válido"),
    categoryId: z.string().cuid2().optional(),
});
export type CreateTierDTO = z.input<typeof createTierSchema>; // → contracts/ICreateTierUseCase Input

export const savePackingSchema = z
    .object({
        items: z.array(z.object({ tierId: z.string().cuid2(), quantity: z.number().int().positive() })),
        looseItems: z.array(z.object({ name: z.string().trim().min(1), costReais: z.number().positive() })),
    })
    .refine((v) => v.items.length + v.looseItems.length > 0, { message: "Inclua ao menos um item" });
export type SavePackingDTO = z.input<typeof savePackingSchema>;
// SavePackingUseCase Input = SavePackingDTO & { orderId: string; operatorId: string }  (DTO+, §6.1)

// idem para os demais endpoints com entrada validada: createCategorySchema → CreateCategoryDTO,
// renameCategorySchema → RenameCategoryDTO, updateTierSchema → UpdateTierDTO,
// setManualAdSpendSchema → SetManualAdSpendDTO, setFixedCostSchema → SetFixedCostDTO,
// e periodQuerySchema (+status) → ListOrdersDTO / o Input dos use cases de relatório.
```

> Os schemas concretos acima (`createTierSchema`, `savePackingSchema`, etc.) são exemplos do padrão e **pertencem aos RFs de cada domínio** — este RF entrega apenas `periodQuerySchema` + a convenção documentada.

**Documentação de API (§15):**

> - **Zod é a fonte da verdade** de validação **e** de doc. Schemas em `src/lib/schemas.ts`; documento em `src/lib/openapi.ts` (`zod-openapi`), servido por `GET /api/openapi.json`; UI Scalar em `/api/reference`.
> - Ao criar/alterar endpoint: atualizar schema + registrar caminho/erros no `openapi.ts`. Documentar também as **respostas de erro** (códigos da §9).

Catálogo de RFs (§18.2 — linha RF-03):

> **RF-03** | Base de documentação/validação: `src/lib/schemas.ts` (com `periodQuerySchema` + convenção de DTO `z.input`), `src/lib/openapi.ts`, rotas `/api/openapi.json` e `/api/reference` (Scalar). | Depende: RF-01 | DoD: `/api/reference` abre documento válido (vazio); `periodQuerySchema` exportado. **Testes:** não.

## 9. Referências

- [`TECH_SPEC.md`](../TECH_SPEC.md) §2 (period query scheme), §8 (schemas + convenção DTO), §9 (códigos de erro a documentar), §15 (zod-openapi + Scalar), §18.2 (catálogo).
- [`PRD.md`](../PRD.md) §5 (F2a/F3/F4 — seletor de período).
- [`CLAUDE.md`](../../CLAUDE.md) — Zod como fonte da verdade de validação e OpenAPI; um export por arquivo; handlers `route.ts` (exceção ao "sem default export").
- Arquivos a estender: `src/lib/schemas.ts`, `src/lib/openapi.ts`, `src/app/api/openapi.json/route.ts`, `src/app/api/reference/route.ts`.
- **Next 16:** route handlers — ler os guias em `node_modules/next/dist/docs/` antes de escrever (`AGENTS.md`).
