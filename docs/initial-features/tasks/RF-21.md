# RF-21 — Impressão direta de etiqueta (QZ Tray)

> **Subtask de implementação.** Autocontido: este arquivo traz contexto, escopo fechado, início (pré-condição), fim (Definition of Done), testes que o cobrem e os trechos exatos da PRD e da TECH_SPEC. Implemente lendo **só este arquivo** (+ as seções referenciadas da PRD/TECH_SPEC) e siga `CLAUDE.md`/`AGENTS.md`.

| | |
| --- | --- |
| **Tipo** | front-end / infra |
| **Onda** | **adiado** |
| **Depende de** | RF-08, RF-11 (devem estar `DONE` em `progress-claude.txt`) |
| **Habilita** | — (recurso opcional; não desbloqueia nenhum RF) |
| **Hotspots tocados** | **modal de Configurações** (RF-11) — nova aba/seção de impressora; **fluxo de impressão da etiqueta** (RF-08/RF-09). Nenhum hotspot de back (sem schema/rota/openapi). |

## 1. Objetivo (o que este RF entrega)

Impressão **silenciosa** de cartelas de código de barras numa impressora térmica de etiquetas (sem diálogo a cada impressão), como **alternativa** ao diálogo do navegador, para gerar várias cartelas rápido (negócio). Integração do **QZ Tray** (agente desktop, ZPL/ESC-POS) com a impressora configurada/persistida no **modal de Configurações**, com **fallback gracioso** para `window.print()` quando não há impressora configurada (técnico).

## 2. Contexto (por quê)

O código de barras é **por faixa de custo**: imprime-se **uma cartela por faixa** ("Caneta R$1", "Caderno R$8") que fica na bancada e, ao bipar, soma +1 daquela faixa ao pedido (PRD F6/D12). No MVP (RF-08/RF-09) a impressão usa `window.print()` + CSS `@page` — funciona em qualquer impressora, mas abre o **diálogo do navegador** a cada vez, o que atrapalha quando se quer gerar **várias cartelas em sequência**. A PRD prevê que a impressão seja **configurável** (RN-6.4): impressão **direta** numa térmica configurada **ou** o diálogo do navegador como alternativa (D13). Este RF é **opcional** e adiado — entra depois de RF-08/RF-11, sem bloquear nada — porque a impressão direta a partir do navegador é limitada e o QZ Tray exige **agente desktop + certificado** (TECH_SPEC §17).

## 3. Início — pré-condições (o que já precisa existir)

> **🚧 GATILHO / GATE (opcional):** este RF é **opcional** e só faz sentido após RF-08 e RF-11 estarem `DONE`, **e** após a decisão de adotar a impressão térmica direta (aquisição/instalação do **agente QZ Tray** no desktop da bancada + **certificado** de assinatura). Enquanto não houver essa decisão/agente, o MVP `window.print()` (RF-08/RF-09) atende o produto — **não inicie**.

Antes de começar, confira no repo (`progress-claude.txt` em `DONE`):

- **RF-08 `DONE`** — `IBarcodeRenderer` + `BwipBarcodeRenderer` (SVG Code128) + `RenderTierLabelUseCase` + rota `GET /api/tiers/:id/label` (`image/svg+xml`). A etiqueta já é renderizável.
- **RF-11 `DONE`** — componente `Modal` reutilizável (composition + animações, §13.3) + **modal de Configurações** (aba "Custos fixos" + Perfil do Clerk). É **onde a configuração de impressora vai morar** (RN-7.3 prevê abas próprias para impressora).
- **RF-09 `DONE`** (recomendado) — página Categorias com a impressão MVP (`window.print` + `@page`) já existente, que este RF passa a ter como **fallback**.

## 4. Escopo — entregáveis (faça exatamente isto)

- **Integração QZ Tray** (agente desktop, ZPL/ESC-POS) como **alternativa** ao diálogo do navegador: conexão ao agente local, listagem de impressoras disponíveis e envio do conteúdo da etiqueta (ZPL/ESC-POS) para **impressão silenciosa**. Assinatura/certificado conforme exigência do QZ Tray (ver §17). Encapsular num módulo/serviço de front próprio (ex.: `src/lib/printing/` ou `src/service/`), seguindo a inversão de dependência do front (o consumidor depende de uma interface, não do SDK direto).
- **Config de impressora persistida no modal de Configurações** (coordenar com o modal de RF-11): nova aba/seção "Impressora" onde o operador escolhe a impressora térmica e o modo de impressão (direta vs diálogo). A persistência segue o padrão das outras configs do modal (via service → hook). _(Nota: o `AppConfig` singleton de RF-01/RF-10 cobre custo fixo; se a impressora precisar de persistência no back, alinhar com a config existente sem inventar schema fora do que já existe — preferir, quando possível, persistência local do dispositivo, já que a impressora é por bancada.)_
- **Fallback gracioso** para `window.print()` (o MVP de RF-08/RF-09) **quando não há impressora configurada** ou o agente QZ Tray está indisponível (RN-6.4). O toque/diálogo **nunca** deixa de funcionar.
- **UX/erros** via toast (sonner), em PT-BR amigável (CLAUDE.md/§13.2): agente offline, impressora não encontrada etc. — **nunca** travar nem mostrar texto técnico.

### Fora do escopo (NÃO faça)

- **NÃO** mexer no back-end (sem schema/migração nova, sem rota nova, sem OpenAPI). A etiqueta já vem de `GET /api/tiers/:id/label` (RF-08).
- **NÃO** reescrever o componente `Modal` nem a aba "Custos fixos" de RF-11 — apenas **acrescentar** a aba/seção de impressora.
- **NÃO** remover o fluxo `window.print()` do MVP — ele vira o **fallback** (default sem impressora).
- **NÃO** mexer na **geração** do código (nanoid) nem no **bipe** (`useBarcodeScanner`) — são de RF-07/RF-15.
- **NÃO** começar sem o gatilho da §3 (agente QZ Tray + certificado decididos).

## 5. Fim — Definition of Done

- Com o agente QZ Tray instalado e uma impressora térmica configurada no modal de Configurações, imprimir a cartela de uma faixa gera a etiqueta **direto na impressora, sem diálogo** (ZPL/ESC-POS).
- **Sem** impressora configurada (ou agente indisponível), o fluxo cai **graciosamente** no `window.print()` do navegador (RN-6.4) — o operador sempre consegue imprimir.
- A configuração de impressora é feita/persistida pelo **modal de Configurações** (aba própria), coordenada com RF-11, sem quebrar a aba "Custos fixos" nem o Perfil.
- Erros (agente offline, impressora ausente) aparecem como **toast amigável PT-BR**; nada trava.
- `bun run lint` limpo; sem `any`. Tela ligada via service/hook (sem seed/Zustand-mock), conforme DoD de front.

## 6. Testes que cobrem este RF (§14.2)

**Não há testes automatizados.** É um RF de front/infra (§14: só casos de uso e entidades/domain services têm testes; RFs de front não geram suíte automatizada). Verificação **manual** + `bun run lint` + checagem de tipos: imprimir direto com impressora configurada e validar o fallback para `window.print()` sem impressora.

## 7. Trecho da PRD que fala deste RF

> ### F6 — Código de barras (gerar, imprimir e bipar)
>
> **História:** _Como operador, quero imprimir uma cartela de código de barras por faixa e deixá-la na bancada, para bipar e somar +1 daquela faixa ao pedido sem precisar procurar o botão na tela._
>
> **Modelo:** o código é **por faixa de custo**, não por unidade física. Cada faixa tem **um** código (gerado na F1). Imprime-se **uma cartela por faixa** (ex.: "Caneta R$1", "Caderno R$8") que fica na bancada; **bipar a cartela equivale a apertar +** daquela faixa. Não se etiqueta produto a produto.
>
> **Requisitos:**
>
> - **Gerar** o código de barras visual da faixa (a partir do código único criado na F1), pronto para impressão como etiqueta/cartela.
> - **Imprimir** a etiqueta da faixa **conectando-se à impressora**, com dois modos **configuráveis**:
>     - **Impressão direta** numa impressora de etiquetas (térmica) já configurada — idealmente sem diálogo a cada impressão, para gerar várias cartelas rápido.
>     - **Diálogo do navegador** como alternativa, quando não há impressora configurada (serve para qualquer impressora).
> - **Bipar (entrada por leitor):** o leitor funciona como teclado e digita o código no campo de leitura da tela de empacotar (F2). Ao reconhecer o código, **soma +1 da faixa correspondente** ao pedido — **a faixa da categoria que foi cadastrada na página de Categorias (F1)**.
> - O leitor é um acessório de hardware a ser adquirido ("bipador" _keyboard wedge_); o produto precisa funcionar com ele **sem driver/SDK** (digita o código + Enter).
>
> **Regras de negócio:**
>
> - **RN-6.1 (um código por faixa):** cada faixa tem exatamente um código de barras único; bipá-lo soma **+1** daquela faixa (igual ao botão **+**).
> - **RN-6.2 (origem do item bipado):** o item somado é o da **faixa cujo código foi bipado**, com a **categoria e o custo daquela faixa** no momento — entra no empacotamento como qualquer item contado (e congela ao concluir, RN-2.8).
> - **RN-6.3 (código não encontrado):** se o código não corresponder a nenhuma faixa, mostrar mensagem amigável e **não** travar — o operador continua pelo toque (mesma regra de RN-2.9).
> - **RN-6.4 (impressão configurável):** o modo de impressão (direta vs diálogo do navegador) é uma configuração; sem impressora configurada, cai no diálogo do navegador.
> - **RN-6.5 (nunca obrigatório):** bipar é **opcional** e convive com o toque; o toque (botões − / +) nunca deixa de funcionar.
>
> **Critérios de aceite:**
>
> - Na faixa "Caneta R$1", cliquei em imprimir → saiu a cartela com o código de barras na impressora de etiquetas configurada (ou abriu o diálogo do navegador, se não houver impressora configurada).
> - Na tela de empacotar, bipei a cartela "Caderno R$8" → entrou +1 item da faixa "Caderno R$8" (categoria Cadernos), e o custo/margem recalcularam.
> - Bipei um código inexistente → recebi aviso amigável e nada foi adicionado; continuei contando pelo toque normalmente.

Da **§7 — Decisões registradas** (PRD):

> | D13 | Impressão da etiqueta é **configurável**: **impressão direta** numa impressora de etiquetas conectada **ou** o **diálogo do navegador** como alternativa.                                  |

## 8. Trecho da TECH_SPEC que fala deste RF

Da **§1 — Decisões técnicas**:

> | TS13 | **Código de barras:** símbolo **Code128**, código único via **nanoid** (+ `@unique` no banco com retry), render **SVG no servidor com bwip-js**. Scanner **keyboard-wedge** via hook custom. Impressão MVP `window.print()`+`@page`; **QZ Tray** depois. |

Da **§11 — Código de barras**:

> - **Render da etiqueta (imagem):** `IBarcodeRenderer` → `BwipBarcodeRenderer` usa **bwip-js** `toSVG({ bcid: "code128", text, includetext: true })` no servidor. Endpoint `GET /api/tiers/:id/label` devolve `image/svg+xml`. A página de etiqueta é Server Component; só o botão de imprimir é client.
> - **Impressão:** **MVP** = `window.print()` + CSS `@media print` com `@page { size: 50mm 30mm; margin: 0 }`. **Depois (QZ Tray)** = impressão silenciosa ZPL/ESC-POS via agente desktop (config de impressora moraria no modal de Configurações). Ver risco §17.
> - **Libs:** `bun add bwip-js nanoid`.

Da **§12 — Autenticação (Clerk)** — onde o modal lê o perfil (contexto da aba):

> - **UI custom (não usar componentes prontos):** sign-in com `useSignIn().sso({ strategy: "oauth_google", ... })` + página `/sso-callback`. **Modal de Configurações** lê `useUser()` (nome/email/foto) e sai com `useClerk().signOut()`. Estilizar com nossos primitivos (Tailwind), sobrepondo o `appearance` quando algum componente do Clerk for usado.

Da **§13.4 — Telas afetadas**:

> - **Modal de Configurações** no rodapé da sidebar: aba **"Custos fixos"** (custo fixo por pedido) + **Perfil** (nome/email/foto do Clerk, sair).

Da **§16 — Ordem de construção**:

> 8. **Impressão direta (QZ Tray):** opcional, depois.

Da **§17 — Riscos e pendências**:

> - **Impressão direta** numa térmica a partir do navegador é limitada; MVP usa o diálogo do navegador. QZ Tray exige agente desktop + certificado.

Da **§18.2 — Catálogo de RFs** (linha-espinha deste RF):

> | **RF-21** | **Impressão direta** (opcional): QZ Tray (ZPL/ESC-POS) como alternativa ao diálogo do navegador, config de impressora no modal. | RF-08, RF-11 | etiqueta + modal | impressão silenciosa funcionando. **Testes:** não. |

## 9. Referências

- PRD: [`../PRD.md`](../PRD.md) — F6 (RN-6.4/RN-6.5), §7 (D13), §8 (pendência de impressão direta).
- TECH_SPEC: [`../TECH_SPEC.md`](../TECH_SPEC.md) — §1 (TS13), §11 (código de barras/impressão), §13.3/§13.4 (Modal/Configurações), §16 (milestone 8), §17 (riscos).
- [`../../CLAUDE.md`](../../CLAUDE.md) — arquitetura de front (service → hook), DIP funcional, primitivos de UI/`components/ui`, erros via toast (sonner) em PT-BR.
- [`./README.md`](./README.md) — DoD geral de front, hotspots, ordem (adiados).
- QZ Tray — documentação do agente desktop, assinatura/certificado e API de impressão ZPL/ESC-POS (validar versão/licença no momento da implementação).
- Guias do Next em `node_modules/next/dist/docs/` (Next 16 com breaking changes) — Client Components para o botão/seção de impressão.
