# RF-17 — Histórico (linhas + resumo + export CSV)

> Subtask autocontida. Um agente deve conseguir implementar este RF lendo só este arquivo + as seções referenciadas da PRD/TECH_SPEC.

| | |
| --- | --- |
| **Tipo** | back-end (Histórico) |
| **Onda** | 3 |
| **Depende de** | RF-14, RF-16 (devem estar `DONE` em `progress-claude.txt`) |
| **Habilita** | RF-19 (front relatórios) |
| **Hotspots tocados** | `src/server/di/container.ts`, `src/server/di/symbols.ts`, `src/lib/schemas.ts`, `src/lib/openapi.ts` |

## 1. Objetivo (o que este RF entrega)

A leitura do **Histórico por período**: as linhas de pedidos empacotados com **CPA, custo fixo e margem líquida**, o **resumo** do período (nº de pedidos, receita, custo, total de ads, lucro, margem média) e a **exportação para CSV**. Tecnicamente: o read gateway `IReportPersistenceGateway` + `ReportPrismaPersistenceGateway`, o `GetHistoryUseCase` (usa o `PeriodReportCalculator` do RF-16) e o `ExportHistoryUseCase` (CSV), com as rotas `/api/history` (GET) e `/api/history/export` (GET `text/csv`).

## 2. Contexto (por quê)

O dono quer ver os pedidos empacotados do período com a margem **já líquida** de anúncios e custo fixo, e exportar para Excel — para enxergar o lucro real de cada pedido. O Histórico é **compartilhado** entre aparelhos (RN-3.7): empacotamentos feitos/editados/excluídos por um operador aparecem para o outro. Os números usam os valores **congelados** no empacotamento (RN-3.5), então editar uma faixa depois não muda o histórico. Este RF é puro back-end de leitura: monta `OrderInPeriod[]` (read gateway), delega o cálculo ao `PeriodReportCalculator` e serializa em `HistoryRow[]` + resumo + CSV.

## 3. Início — pré-condições (o que já precisa existir)

- **RF-14 `DONE`:** entidade/repo `Packing` + `SavePackingUseCase` (snapshot congelado: `tierName`, `categoryName`, `unitCostCents`, `quantity` em `PackingItem`; `LooseItem`); pedidos podem estar `PACKED`. As linhas do histórico vêm dos empacotamentos persistidos.
- **RF-16 `DONE`:** `PeriodReportCalculator` (CPA, margem líquida por pedido, lucro do período) e o ad spend por período (`GetAdSpendUseCase`/`IAdSpendPersistenceGateway`) — o total de ads do período alimenta o CPA. `IConfigPersistenceGateway`/`GetFixedCostUseCase` (custo fixo) via RF-10.
- **RF-01:** `Money`, `Period` (SP), `DomainError`/`toHttpResponse`, container + symbols, banco de teste + helpers.
- **RF-03:** `periodQuerySchema` em `src/lib/schemas.ts`; `src/lib/openapi.ts`.
- Como conferir: existe ao menos um pedido `PACKED` no banco de teste (`givenOrder` + `SavePackingUseCase`); `GET /api/ad-spend?period=today` devolve total.
- **Não altere `prisma/schema.prisma`** (criado só pelo RF-01).

## 4. Escopo — entregáveis (faça exatamente isto)

- `src/server/application/gateway/IReportPersistenceGateway.ts` — porta de leitura: dado um `Period`, devolve os pedidos do período com o detalhamento necessário para o cálculo (venda, frete, `itemsCost` — `null` se não empacotado —, e os itens com `categoryName`/`cost` **congelados**), no formato que vira `OrderInPeriod[]` do `PeriodReportCalculator`.
- `src/server/infrastructure/gateway/ReportPrismaPersistenceGateway.ts` — implementação Prisma (join `Order` + `Packing` + `PackingItem`/`LooseItem`); `mapToEntity`/mapeamento privado no fim da classe se preciso.
- `src/server/application/use-case/GetHistoryUseCase.ts` + `.../contracts/IGetHistoryUseCase.ts` — `period` → `{ rows: HistoryRow[], summary }`. Lê os pedidos do período (read gateway), o total de ads (RF-16) e o custo fixo (RF-10), usa `PeriodReportCalculator.computeCpa`/`computeNetMarginPerOrder`/`computePeriodProfit`, serializa para `HistoryRow[]` (centavos) + resumo.
- `src/server/application/use-case/ExportHistoryUseCase.ts` + `.../contracts/IExportHistoryUseCase.ts` — `period` → **CSV** (string) com as colunas da RN-3.6.
- `src/app/api/history/route.ts` — `GET` (period query) → `{ rows, summary }`. Auth (Clerk) + `toHttpResponse` no `catch`.
- `src/app/api/history/export/route.ts` — `GET` (period query) → `Content-Type: text/csv` (download). Auth + `toHttpResponse`.
- `src/lib/schemas.ts` — reusar `periodQuerySchema` (export do DTO de período se a convenção ainda não existir para os use cases de relatório).
- `src/lib/openapi.ts` — registrar `GET /api/history` (resposta `{ rows, summary }`) e `GET /api/history/export` (`text/csv`).
- `src/server/di/container.ts` + `symbols.ts` — bindings de `IReportPersistenceGateway`/`ReportPrismaPersistenceGateway`, `GetHistoryUseCase`, `ExportHistoryUseCase`.

### Fora do escopo (NÃO faça)

- Não implementar `PeriodReportCalculator` nem o ad spend — já vêm do RF-16 (só **consumir**).
- Não implementar `GetDashboardUseCase` nem `/api/dashboard` — RF-18 (mesmo que possa **reusar** o `IReportPersistenceGateway`; quem define se RF-18 reusa essa porta é o RF-18, ver lá).
- Não tocar o front (tabela/export do Histórico) — RF-19.
- Não alterar `prisma/schema.prisma`.

## 5. Fim — Definition of Done

- `GET /api/history?period=...` devolve `{ rows: HistoryRow[], summary }`: cada linha com `cpaCents`, `fixedCostCents`, `netMarginCents`/`netMarginPct` (`null` quando não empacotado); CPA = ads ÷ nº de pedidos (e 0 quando 0 pedidos); margem líquida por pedido = `venda − custo − frete − CPA − custo fixo`; resumo (receita/custo/lucro/margem média/total de ads) coerente; usa os valores **congelados**.
- `GET /api/history/export?period=...` devolve **CSV** (`text/csv`) com as colunas: **Data, Cliente, Hora, Venda, Custo, CPA, Custo fixo, Margem R$, Margem %** (RN-3.6).
- O cálculo é delegado ao `PeriodReportCalculator` (RF-16); o use case **não** reimplementa as fórmulas.
- Schema/DTO de período e OpenAPI (§15) atualizados.
- **Testes da §14.2 passando** (`bun test`). `bun run lint` limpo; sem `any`.

## 6. Testes que cobrem este RF (§14.2)

Integração (`test/application/*.test.ts`, banco de teste + fakes externos):

- **`GetHistoryUseCase`** — por período: **CPA = ads ÷ nº de pedidos** (e CPA=0 quando 0 pedidos); **margem líquida por pedido = venda − custo − frete − CPA − custo fixo**; resumo (receita/custo/lucro/margem média) coerente; usa os valores **congelados**.
- **`ExportHistoryUseCase`** — gera CSV do período com as colunas certas (Data, Cliente, Hora, Venda, Custo, CPA, Custo fixo, Margem R$, Margem %).

## 7. Trecho da PRD que fala deste RF

> ### F3 — Histórico (por período)
>
> **História:** _Como dono, quero ver os pedidos empacotados do período com a margem já líquida de anúncios e custo fixo, e exportar para Excel, para enxergar o lucro real de cada pedido._
>
> **Requisitos:**
>
> - Lista dos pedidos empacotados com: data, @cliente, hora, valor da venda, custo dos itens, **CPA**, **custo fixo**, **margem R$** e **margem %**.
> - **Nova coluna "CPA"** por pedido (custo de anúncio por pedido — ver RN-3.2).
> - **Resumo do período (cards):** nº de pedidos, receita, custo, **total de ads do período** (já existe e está correto), lucro e **margem média**.
> - Seleção de período (hoje / semana / mês / personalizado) — mesma lógica do Dashboard.
> - **Exportar para CSV/Excel** os pedidos do período.
>
> **Regras de negócio:**
>
> - **RN-3.1 (CPA por pedido):** `CPA = total de ads do período ÷ nº de pedidos vendidos no período`. É o mesmo valor para todos os pedidos do período; quanto mais pedidos, menor o CPA de cada um (é literalmente o **custo por aquisição**). O total de ads vem do TikTok (F5).
> - **RN-3.2 (margem líquida por pedido):** `margem R$ = valor da venda − custo dos itens − frete − CPA − custo fixo por pedido`. `margem % = (margem R$ ÷ valor da venda) × 100`. (O custo fixo vem das Configurações — F7/RN-7.x; hoje R$3.)
> - **RN-3.3 (lucro do período):** `lucro = receita − custo dos itens − frete − total de ads − (custo fixo × nº de pedidos)`. `margem média = (lucro ÷ receita) × 100`. Repare que `Σ CPA = total de ads`, então a soma das margens por pedido reconcilia com o lucro do período.
> - **RN-3.4 (editar/excluir empacotamento):** é possível **editar** o empacotamento de um pedido (recontar itens/avulsos) e **excluir** o empacotamento (o pedido volta a "pendente / não empacotado"). Cliente, valor e frete **não** se editam aqui (vêm do TikTok). Ao editar, custo e margem são recalculados e o snapshot regravado.
> - **RN-3.5 (histórico congelado):** editar/excluir **faixas ou categorias** depois **não altera** pedidos já empacotados. Cada empacotamento mantém o custo, o nome da faixa e a categoria **da época**.
> - **RN-3.6 (export):** o arquivo contém, por pedido: Data, Cliente, Hora, Venda, Custo, CPA, Custo fixo, Margem R$, Margem %. (Formato CSV vs `.xlsx` formatado → tech spec.)
> - **RN-3.7 (sincronização):** o histórico é **compartilhado** — empacotamentos feitos/editados/excluídos por um operador aparecem para o outro.

Decisão registrada (D18):

> | D18 | Histórico ganha coluna **CPA** = `total de ads do período ÷ nº de pedidos do período`; a **margem por pedido** desconta **CPA + custo fixo**. |

## 8. Trecho da TECH_SPEC que fala deste RF

Inventário de casos de uso (§6.2):

> | `GetHistoryUseCase` (DTO)       | period → linhas (com CPA, custo fixo, margem líquida) + resumo             | (validação de período)                                                                |
> | `ExportHistoryUseCase` (DTO)    | period → CSV                                                               | (validação de período)                                                                |

PersistenceGateway (§7):

> | PersistenceGateway  | `application/gateway/IReportPersistenceGateway`         | `infrastructure/gateway/ReportPrismaPersistenceGateway`        |

Endpoints (§8):

> | GET    | `/api/history`                       | Linhas + resumo do período (com CPA e custo fixo)               | `periodQuery`                                   | `{ rows: HistoryRow[], summary }`         |
> | GET    | `/api/history/export`                | CSV do período                                                  | `periodQuery`                                   | `text/csv`                                |

DTO de leitura — `HistoryRow` (§8):

> ```ts
> type HistoryRow = {
>     orderId: string; orderNumber: string; recipientName: string | null; orderedAt: string;
>     saleCents: number; itemsCostCents: number | null; cpaCents: number; fixedCostCents: number;
>     netMarginCents: number | null; netMarginPct: number | null;
> };
> ```

Period query scheme (§2):

> ```ts
> // @/lib/period.ts  — schema compartilhado
> export const periodQuerySchema = z
>     .object({
>         period: z.enum(["today", "week", "month", "custom"]),
>         from: z.iso.date().optional(),
>         to: z.iso.date().optional(),
>     })
>     .refine((v) => v.period !== "custom" || (v.from && v.to), {
>         message: "Período personalizado exige 'from' e 'to'",
>         path: ["from"],
>     });
> ```

Linha do catálogo (§18.2):

> | **RF-17** | Histórico: `IReportPersistenceGateway` (read) + `GetHistoryUseCase` (linhas + CPA + custo fixo + margem líquida + resumo) + `ExportHistoryUseCase` (CSV) + rotas. | RF-14, RF-16 | empacotamentos + calculator | endpoints `/api/history*` corretos. **Testes:** integração `GetHistoryUseCase` e `ExportHistoryUseCase` (§14.2). |

## 9. Referências

- PRD: [`../PRD.md`](../PRD.md) — F3 completa (RN-3.1..3.7), D18.
- TECH_SPEC: [`../TECH_SPEC.md`](../TECH_SPEC.md) — §6.2, §7 (`IReportPersistenceGateway`), §8 (`/api/history*`, `HistoryRow`), §2 (`periodQuerySchema`), §5.3 (`PeriodReportCalculator`).
- [`../../CLAUDE.md`](../../CLAUDE.md) — PersistenceGateway, padrão de use case, DIP, modelo de erros.
