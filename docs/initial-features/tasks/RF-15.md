# RF-15 — Tela de empacotamento (front)

> Subtask autocontida. Um agente deve conseguir implementar este RF lendo só este arquivo + as seções referenciadas da PRD/TECH_SPEC.

| | |
| --- | --- |
| **Tipo** | **front-end** (empacotamento) |
| **Onda** | 3 |
| **Depende de** | RF-02, RF-07, RF-13, RF-14 (devem estar `DONE` em `progress-claude.txt`) |
| **Habilita** | — (fecha o fluxo de empacotamento fim-a-fim) |
| **Hotspots tocados** | `src/di/container.ts`, `src/di/symbols.ts` (service + hooks de packing/faixa) |

## 1. Objetivo (o que este RF entrega)

O operador abre um pedido, conta faixas e avulsos (toque **ou** bipe), vê a **margem ao vivo** (R$ e %) e conclui o empacotamento — tudo contra a API real. Tecnicamente: a página `src/app/pedidos/[orderId]/empacotar/page.tsx` (Server Component) com client islands refatorados dos `src/components/pack/*`, o hook `useBarcodeScanner`, o service + query (`GET /api/orders/:id/packing`) e mutations (`PUT`/`DELETE /api/orders/:id/packing`), cabeçalho read-only e painel de margem ao vivo, trocando o seed/mock por rascunho real.

## 2. Contexto (por quê)

É o **coração do produto** (PRD §6): rápido, mobile-first, margem em destaque, áreas de toque grandes. Nesta fase a tela parte de um **pedido vindo do TikTok**: cliente, valor da venda e frete são **somente leitura** (D17 / RN-2.5) — o operador não digita esses campos. A margem ao vivo é venda − custo − frete (sem CPA/custo fixo — RN-2.5). O **bipe** (F6) é entrada opcional que **convive com o toque**: bipar a cartela de uma faixa soma +1 daquela faixa (RN-6.1/6.5); código não encontrado mostra aviso amigável e **não** trava (RN-2.9/RN-6.3). "Concluir empacotamento" salva via `PUT` e exige ≥1 item (RN-2.7). Hoje tudo isso roda com seed em memória (Zustand-mock) — este RF troca por estado de rascunho real + hooks de query/mutation.

## 3. Início — pré-condições (o que já precisa existir)

- **RF-02 `DONE`:** `HttpClient` front, `ApiError` + dicionário PT, `QueryClient` `onError` global (toast). Container front.
- **RF-07 `DONE`:** rota `GET /api/tiers/by-barcode/:code` (resolve bipe → faixa).
- **RF-13 `DONE`:** página `src/app/pedidos/page.tsx` + navegação para `/pedidos/[orderId]/empacotar` + `OrderService`/`use-orders` + tabela componentizada.
- **RF-14 `DONE`:** rotas `GET/PUT/DELETE /api/orders/:id/packing` (snapshot + PACKED + regras).
- Existente no repo (a refatorar): `src/components/pack/*` (barcode-scanner, loose-item-chips, loose-item-form, order-details-form, packing-summary-bar, packing-toolbar, tier-catalog, tier-counter-card), `src/stores/use-packing-store.ts`, `src/hooks/use-barcode-scan.ts`, `src/utils/seed.ts`.
- Como conferir: `curl /api/orders/<id>/packing` devolve `{ order, packing? }`; `curl /api/tiers/by-barcode/<code>` resolve a faixa.

## 4. Escopo — entregáveis (faça exatamente isto)

**Página (Server Component) + client islands:**

- `src/app/pedidos/[orderId]/empacotar/page.tsx` — **Server Component**: lê o `orderId` da rota, escreve o chrome estático (título) e passa o `orderId` para os client islands via props. Sem um único `*-screen.tsx` raiz "use client".
- Refatorar os `src/components/pack/*` em **client islands** ligados à API (não ao seed):
    - **Cabeçalho read-only** (substitui `order-details-form.tsx`): mostra @cliente/`recipientName`, **valor da venda** e **frete** vindos do pedido — sem inputs editáveis (D17).
    - **Contagem de faixas** (`tier-catalog.tsx` + `tier-counter-card.tsx`): faixas agrupadas por categoria, botões − / + grandes.
    - **Itens avulsos** (`loose-item-form.tsx` + `loose-item-chips.tsx`): adicionar nome + custo.
    - **Bipe** (`barcode-scanner.tsx` + toolbar): campo de leitura + `useBarcodeScanner`.
    - **Painel de margem ao vivo** (substitui `packing-summary-bar.tsx`): custo dos itens, contagem total, **margem em R$ e %** (sem CPA/custo fixo); botão **"Concluir empacotamento"**.

**Hook de bipe (scanner keyboard-wedge):**

- `src/hooks/use-barcode-scanner.ts` — `useBarcodeScanner(onScan)`: escuta `keydown` no `window`, acumula caracteres rápidos, dispara `onScan(code)` no Enter, descartando digitação humana (intervalo > ~50ms) e bursts < 4 chars. Substitui/atualiza o atual `src/hooks/use-barcode-scan.ts`. `onScan` resolve via `GET /api/tiers/by-barcode/:code` e faz `incrementTier` no rascunho; código não encontrado → toast amigável, **sem** travar (toque continua).

**Camada de dados (service → query/mutation):**

- `src/service/packing-service.ts` — factory recebendo o `HttpClient`: `getForPacking(orderId)` → `http.get(.../packing)`, `save(orderId, input)` → `http.put(.../packing, input)`, `remove(orderId)` → `http.delete(.../packing)`. (Reusar/estender o `tier-service` para `byBarcode(code)` → `GET /api/tiers/by-barcode/:code`.)
- `src/hooks/query/use-order-packing.ts` — `useQuery` (`queryKey` `["order-packing", orderId]`) pedindo o service por interface.
- `src/hooks/mutation/use-save-packing.ts` + `src/hooks/mutation/use-delete-packing.ts` — `useMutation`; **invalidar** as tags `["order-packing", orderId]` e `["orders", ...]` no sucesso.
- `src/di/container.ts` + `src/di/symbols.ts` — **acrescentar** binding do `PackingService`.

**Rascunho (Zustand só para estado genuinamente compartilhado):**

- Substituir o `use-packing-store.ts` (seed/mock) por um store de **rascunho real** (contagem de faixas + avulsos + campo de bipe) — Zustand só porque o rascunho é compartilhado entre os islands (catálogo, scanner, painel). Seed cliente/venda/frete **removido** (vêm do pedido, read-only).
- Ao concluir, mapear o rascunho para o `SavePackingInput` (`{ items: [{ tierId, quantity }], looseItems: [{ name, costReais }] }`) e chamar a mutation `PUT`.

### Fora do escopo (NÃO faça)

- **Não** alterar o back (entidade `Packing`, use cases, rotas) — RF-14 já entregou.
- **Não** implementar a lista de Pedidos nem a tabela componentizada — RF-13.
- **Não** implementar etiqueta/impressão (`window.print`) — RF-08/RF-09.
- **Não** adicionar CPA/custo fixo na margem ao vivo (são de período — RF-17/18/19).
- **Sem testes automatizados** (RF de front — §14).

## 5. Fim — Definition of Done

- `/pedidos/[orderId]/empacotar` empacota **fim-a-fim contra a API**: carrega pedido + empacotamento atual, conta faixas/avulsos, conclui via `PUT` e (quando já empacotado) recarrega o detalhamento para editar.
- Cabeçalho **read-only** com cliente/venda/frete do pedido (sem inputs).
- **Bipe** soma +1 da faixa (via `useBarcodeScanner` → `GET /api/tiers/by-barcode/:code` → `incrementTier`); código inexistente → toast amigável, sem travar; toque continua funcionando.
- Painel mostra custo dos itens, contagem e **margem em R$ e %** ao vivo (sem CPA/custo fixo).
- "Concluir empacotamento" exige ≥1 item (a API valida; o front mostra a mensagem amigável); excluir empacotamento volta o pedido a pendente.
- Seed/Zustand-mock **removido**; rascunho via store real; dados via service → hook (query/mutation).
- Páginas são **Server Components** com client islands; medidas relativas (sem `px`); reúso de `components/ui`; sem memo manual (React Compiler).
- Erros via toast (§13.2); `bun run lint` limpo; sem `any`.

## 6. Testes que cobrem este RF (§14.2)

Não há testes automatizados (RF de front-end). Verificação manual + `bun run lint` + checagem de tipos. (Front não tem suíte automatizada nesta fase — §14.)

## 7. Trecho da PRD que fala deste RF

> #### F2b — Tela de empacotamento (de um pedido)
>
> **Requisitos:**
>
> - Aberta por URL com o **id do pedido**.
> - **Cabeçalho somente leitura** com os dados vindos do TikTok: @cliente, **valor da venda** e **frete** (o operador **não digita** esses campos).
> - Faixas agrupadas por categoria, com botões **−** e **+** grandes para contar.
> - Adicionar **itens avulsos** (nome + custo).
> - **Entrada por código de barras (opcional):** o leitor funciona como teclado; o código representa a **faixa**. Bipar incrementa a contagem da faixa (ver F6). Bipar e tocar convivem.
> - **Painel de destaque fixo** mostrando, em tempo real: custo dos itens, contagem total e **margem em R$ e %**.
> - Botão **"Concluir empacotamento"** que salva o empacotamento no pedido.
>
> **Regras de negócio:**
>
> - **RN-2.5 (margem ao vivo):** na tela de empacotamento, `margem R$ = valor da venda − custo dos itens − frete`. `margem % = (margem R$ ÷ valor da venda) × 100`. **Ao vivo não entram Ads/CPA nem custo fixo** — esses são de período e aparecem no Histórico/Dashboard (F3/F4). A margem ao vivo é o termômetro rápido "o conteúdo do kit cabe no preço?".
> - **RN-2.6 (custo dos itens):** soma de (quantidade × custo da faixa) de todas as faixas contadas + soma dos custos dos itens avulsos.
> - **RN-2.7 (concluir):** só é possível concluir com **pelo menos 1 item** (faixa contada ou avulso). O valor da venda já vem do TikTok (sempre presente). Sem itens, mensagem amigável impede a conclusão.
> - **RN-2.8 (snapshot):** ao concluir, o empacotamento **congela** o custo de cada item (faixa e avulso), o detalhamento (faixa + quantidade), o **nome da faixa/categoria da época**, e registra **operador, data e hora**.
> - **RN-2.9 (código de barras não encontrado):** código bipado sem faixa correspondente → mensagem amigável, **não** trava; segue pelo toque.

> ### F6 — Código de barras (gerar, imprimir e bipar)
>
> **Regras de negócio:**
>
> - **RN-6.1 (um código por faixa):** cada faixa tem exatamente um código de barras único; bipá-lo soma **+1** daquela faixa (igual ao botão **+**).
> - **RN-6.3 (código não encontrado):** se o código não corresponder a nenhuma faixa, mostrar mensagem amigável e **não** travar — o operador continua pelo toque (mesma regra de RN-2.9).
> - **RN-6.5 (nunca obrigatório):** bipar é **opcional** e convive com o toque; o toque (botões − / +) nunca deixa de funcionar.

> | D17 | Na tela de empacotamento, **cliente, valor e frete são somente leitura** (TikTok é a fonte da verdade).                                                                                    |

## 8. Trecho da TECH_SPEC que fala deste RF

Telas afetadas (§13.4):

> - **Tela de empacotamento** (`src/app/pedidos/[orderId]/empacotar/`): cabeçalho **read-only** (cliente/venda/frete do pedido); contagem de faixas + avulsos; bipe; painel de margem ao vivo; "Concluir empacotamento" (`PUT /api/orders/:id/packing`).
> - **Sem stores de seed:** trocar Zustand-seed por hooks de query/mutation reais (o catálogo/pedidos/ads passam a vir da API). Zustand fica só para estado de UI genuinamente compartilhado (ex.: rascunho do empacotamento, modal de etiqueta).

Código de barras — bipe (§11):

> - **Bipe (scanner keyboard-wedge):** hook client `useBarcodeScanner(onScan)` (`@/hooks/use-barcode-scanner.ts`) escuta `keydown` no `window`, acumula caracteres rápidos e dispara `onScan(code)` no Enter, descartando digitação humana (intervalo > ~50ms) e bursts < 4 chars. Na tela de empacotamento, `onScan` resolve via `GET /api/tiers/by-barcode/:code` e faz `incrementTier` no rascunho (toque continua funcionando — RN-2.9).

Estrutura por camada (§13.1):

> `component → hook (query|mutation) → service → HttpClient → API`. DI funcional via Inversify (`src/di/container.ts` + `symbols.ts`): services (factory functions) recebem o **`HttpClient`** (factory `axiosHttpClient`, §7.4) por injeção; hooks pedem a interface do service.

> **Localização das camadas no front:** a camada de **service fica em `src/service/`** (diretamente sob `src/`, **não** dentro de `src/hooks/`). `src/hooks/query/` e `src/hooks/mutation/` ficam separados e dependem da interface do service.

Tratamento de erros (§13.2):

> - `QueryClient` com `onError` global (queries e mutations) → `resolveMessage(error)` → `toast.error(...)` (sonner). Hooks específicos podem sobrescrever quando precisarem de UX diferente.

Endpoints consumidos (§8):

> | GET    | `/api/tiers/by-barcode/:code`        | Resolve bipe → faixa                                            | —                                               | `Tier`                                    |
> | GET    | `/api/orders/:id/packing`            | Pedido (read-only) + empacotamento atual                        | —                                               | `{ order, packing? }`                     |
> | PUT    | `/api/orders/:id/packing`            | Cria/edita empacotamento; marca PACKED                          | `SavePackingInput`                              | `Packing`                                 |
> | DELETE | `/api/orders/:id/packing`            | Remove empacotamento; pedido volta a pendente                   | —                                               | `204`                                     |

Payload de conclusão (§8 — `savePackingSchema`):

```ts
export const savePackingSchema = z
    .object({
        items: z.array(z.object({ tierId: z.string().cuid2(), quantity: z.number().int().positive() })),
        looseItems: z.array(z.object({ name: z.string().trim().min(1), costReais: z.number().positive() })),
    })
    .refine((v) => v.items.length + v.looseItems.length > 0, { message: "Inclua ao menos um item" });
export type SavePackingDTO = z.input<typeof savePackingSchema>;
```

Linha do catálogo (§18.2):

> | **RF-15 (front)** | Tela de **empacotamento** (`/pedidos/[orderId]/empacotar`): cabeçalho read-only (cliente/venda/frete), contagem de faixas + avulsos, **bipe** (`useBarcodeScanner` → `FindTierByBarcode`), painel de **margem ao vivo** (R$ e %), "Concluir empacotamento". Remover seed. | RF-02, RF-07, RF-13, RF-14 | endpoints de packing + faixa | empacotar fim-a-fim contra a API; bipe soma faixa. **Testes:** não (front). |

## 9. Referências

- PRD: [`../PRD.md`](../PRD.md) — F2b (RN-2.5..2.9), F6 (RN-6.1, RN-6.3, RN-6.5), D17.
- TECH_SPEC: [`../TECH_SPEC.md`](../TECH_SPEC.md) — §13.4 (tela de empacotamento), §11 (bipe — `useBarcodeScanner`), §13.1 (estrutura por camada), §13.2 (erros), §8 (`/api/orders/:id/packing` + `/api/tiers/by-barcode/:code` + `savePackingSchema`).
- [`../../CLAUDE.md`](../../CLAUDE.md) — Front-end (Server Components + client islands, Zustand só para estado compartilhado, medidas relativas, reúso de `components/ui`, React Compiler sem memo manual), fluxo component → hook → service → HttpClient.
- Guias do Next 16 em `node_modules/next/dist/docs/` (rotas dinâmicas `[orderId]`, Server vs Client Components) — **ler antes** de escrever código de framework (AGENTS.md).
