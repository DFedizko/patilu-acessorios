# RF-06 — Casos de uso de categoria

> Subtask autocontida. Um agente deve conseguir implementar este RF lendo só este arquivo + as seções referenciadas da PRD/TECH_SPEC.

| | |
| --- | --- |
| **Tipo** | back-end (use cases categoria) |
| **Onda** | 1 |
| **Depende de** | RF-03, RF-05 (devem estar `DONE` em `progress-claude.txt`) |
| **Habilita** | RF-07 (faixas), RF-09 (Categorias front) |
| **Hotspots tocados** | `src/server/di/container.ts`, `src/server/di/symbols.ts`, `src/lib/schemas.ts`, `src/lib/openapi.ts`, `src/lib/error-messages.ts` |

## 1. Objetivo (o que este RF entrega)

O CRUD de **categorias** completo: criar, renomear, excluir (sem bloquear — faixas vão para "Sem categoria") e listar o catálogo (categorias + faixas). Tecnicamente: 4 casos de uso seguindo o padrão `*UseCase`, um read gateway para a listagem, as rotas `/api/categories*` validadas por Zod e documentadas no OpenAPI.

## 2. Contexto (por quê)

O operador precisa refletir o sortimento da semana ajustando categorias na hora. Excluir uma categoria com faixas **não pode bloquear** — as faixas migram para "Sem categoria" (RN-1.1 / D10), que não é uma linha no banco, mas `Tier.categoryId = null` resolvido pelo `onDelete: SetNull`. A listagem (`ListCatalogUseCase`) alimenta a tela de Categorias e a de empacotamento (faixas agrupadas por categoria).

## 3. Início — pré-condições (o que já precisa existir)

- **RF-05 `DONE`:** entidades `Category`/`Tier` e repos `ICategoryRepository`/`ITierRepository` + impls Prisma prontos e com bindings de DI.
- **RF-03 `DONE`:** `src/lib/schemas.ts` (com convenção de DTO `z.input` + `periodQuerySchema`), `src/lib/openapi.ts`, rotas `/api/openapi.json` e `/api/reference` (Scalar) prontas.
- Como conferir: container resolve `CategoryRepository`/`TierRepository`; `/api/reference` abre documento válido.
- **Não altere `prisma/schema.prisma`** (criado só pelo RF-01).

## 4. Escopo — entregáveis (faça exatamente isto)

**Casos de uso** (`src/server/application/use-case/` + contrato em `.../contracts/`, padrão §6.1):

- `CreateCategoryUseCase` (DTO) — `{ name }` → Category. Erro: `CATEGORY_NAME_REQUIRED`.
- `RenameCategoryUseCase` (DTO+) — `{ id, name }` → Category (`id` da rota + `name` do corpo). Erros: `CATEGORY_NOT_FOUND`, `CATEGORY_NAME_REQUIRED`.
- `DeleteCategoryUseCase` — `{ id }` → void (faixas → `categoryId = null` via `onDelete: SetNull`). Erro: `CATEGORY_NOT_FOUND`.
- `ListCatalogUseCase` — sem entrada → categorias + faixas (via read gateway), incluindo "Sem categoria".

**Read gateway** (PersistenceGateway, §7):

- `src/server/application/gateway/ICatalogReadPersistenceGateway.ts` — contrato (camada de aplicação).
- `src/server/infrastructure/gateway/CatalogReadPrismaPersistenceGateway.ts` — implementação Prisma (read model `CategoryWithTiers[]`).

**Rotas** (`src/app/api/`, borda HTTP: Zod + Clerk + `toHttpResponse`):

- `src/app/api/categories/route.ts` — `GET` (lista) + `POST` (cria).
- `src/app/api/categories/[id]/route.ts` — `PATCH` (renomeia) + `DELETE` (exclui).

**Schemas/DTOs/OpenAPI/erros (aditivo):**

- `src/lib/schemas.ts` — `createCategorySchema` → `CreateCategoryDTO`; `renameCategorySchema` → `RenameCategoryDTO` (via `z.input`).
- `src/lib/openapi.ts` — registrar caminhos `/api/categories` e `/api/categories/:id` + respostas de erro.
- `src/lib/error-messages.ts` — entradas PT para `CATEGORY_NOT_FOUND`, `CATEGORY_NAME_REQUIRED`.
- `src/server/di/container.ts` + `symbols.ts` — bindings dos 4 use cases + `CatalogReadPersistenceGateway`.

### Fora do escopo (NÃO faça)

- Não criar nada de **faixa** (`CreateTierUseCase`, rotas `/api/tiers*`, barcode) — RF-07.
- Não criar a etiqueta SVG — RF-08.
- Não tocar telas/front — RF-09.
- Não alterar `prisma/schema.prisma`.

## 5. Fim — Definition of Done

- Endpoints `/api/categories*` funcionando e **documentados** no OpenAPI (caminhos + erros).
- Excluir categoria com faixas **não bloqueia**: faixas vão para "Sem categoria" (`null`), nada é apagado.
- Erros `CATEGORY_NOT_FOUND` (404) e `CATEGORY_NAME_REQUIRED` (422) cobertos; `VALIDATION_ERROR` (400) na rota via Zod.
- DTOs de `src/lib/schemas.ts` reusados como `Input` dos contratos (§6.1) — validação e tipagem não divergem.
- **Testes da §14.2 passando** (`bun test`): integração dos 3 casos de uso de categoria.
- `bun run lint` limpo; sem `any`.

## 6. Testes que cobrem este RF (§14.2)

Integração (`test/application/*.test.ts`, banco de teste + fakes):

- **`CreateCategoryUseCase`** — cria e persiste a categoria com o nome dado; **rejeita** nome vazio com `CATEGORY_NAME_REQUIRED`.
- **`RenameCategoryUseCase`** — renomeia a categoria existente; **erro** `CATEGORY_NOT_FOUND` se o id não existe; rejeita nome vazio.
- **`DeleteCategoryUseCase`** — remove a categoria e **as faixas dela passam a `categoryId = null`** (vão para "Sem categoria"), **não** são apagadas (PRD RN-1.1).

> `ListCatalogUseCase` lê do read gateway — coberto indiretamente (não há erro de regra).

## 7. Trecho da PRD que fala deste RF

> ### F1 — Categorias e faixas de custo (CRUD)
>
> **Requisitos:**
>
> - Criar categoria (só nome).
> - Renomear e excluir categoria.

> **Regras de negócio:**
>
> - **RN-1.1** Excluir uma categoria que tem faixas **não bloqueia**: as faixas vão para **"Sem categoria"** (uma categoria implícita/padrão, sempre presente).
> - **RN-1.5** Nome de categoria e de faixa são obrigatórios (não vazios).

> | D10 | Excluir categoria com faixas → faixas vão para **"Sem categoria"** (não bloqueia).                                                                                                         |

## 8. Trecho da TECH_SPEC que fala deste RF

Padrão de use case (§6.1):

```ts
// application/use-case/contracts/IUseCase.ts
export interface IUseCase<TInput, TOutput> {
    execute(input: TInput): Promise<TOutput>;
}
```

```ts
// application/use-case/contracts/ICreateTierUseCase.ts
import type { IUseCase } from "./IUseCase";
import type { CreateTierDTO } from "@/lib/schemas"; // = z.input<typeof createTierSchema>

export type Input = CreateTierDTO; // mesmo contrato tipado do schema Zod do endpoint
export type Output = { id: string; name: string; costCents: number; barcode: string; categoryId: string | null };

export interface ICreateTierUseCase extends IUseCase<Input, Output> {}
```

> Quando o caso de uso precisa de dados que **não** vêm do corpo validado (ex.: `id` da rota, `operatorId` do Clerk), o `Input` é o DTO do schema **acrescido** desses campos (ex.: `Input = SavePackingDTO & { orderId: string; operatorId: string }`); o schema do endpoint valida apenas o que vem do cliente.

Inventário de casos de uso (§6.2):

> | `CreateCategoryUseCase` (DTO)   | `{ name }` → Category                                                      | `CATEGORY_NAME_REQUIRED`                                                               |
> | `RenameCategoryUseCase` (DTO)   | `{ id, name }` → Category                                                  | `CATEGORY_NOT_FOUND`, `CATEGORY_NAME_REQUIRED`                                         |
> | `DeleteCategoryUseCase`         | `{ id }` → void (faixas → null)                                            | `CATEGORY_NOT_FOUND`                                                                   |
> | `ListCatalogUseCase`            | — → categorias + faixas (read gateway)                                     | —                                                                                     |

PersistenceGateway (§7):

> | PersistenceGateway  | `application/gateway/ICatalogReadPersistenceGateway`    | `infrastructure/gateway/CatalogReadPrismaPersistenceGateway`   |

Endpoints (§8):

> | GET    | `/api/categories`                    | Lista categorias com suas faixas (+ "Sem categoria")            | —                                               | `CategoryWithTiers[]`                     |
> | POST   | `/api/categories`                    | Cria categoria                                                  | `{ name }`                                      | `Category` (201)                          |
> | PATCH  | `/api/categories/:id`                | Renomeia                                                        | `{ name }`                                      | `Category`                                |
> | DELETE | `/api/categories/:id`                | Exclui (faixas → Sem categoria)                                 | —                                               | `204`                                     |

Erros (§9):

> | `CATEGORY_NOT_FOUND`           | 404  | repo              |
> | `CATEGORY_NAME_REQUIRED`       | 422  | domínio           |

Linha do catálogo (§18.2):

> | **RF-06** | Casos de uso de categoria: `CreateCategoryUseCase`, `RenameCategoryUseCase`, `DeleteCategoryUseCase` (faixas→null), `ListCatalogUseCase` (read gateway) + rotas + Zod/OpenAPI. | RF-03, RF-05 | repos de catálogo prontos | endpoints `/api/categories*` funcionando e documentados. **Testes:** integração dos 3 casos de uso de categoria (§14.2). |

## 9. Referências

- PRD: [`../PRD.md`](../PRD.md) — F1 (RN-1.1, RN-1.5), D10.
- TECH_SPEC: [`../TECH_SPEC.md`](../TECH_SPEC.md) — §6.1, §6.2, §7 (`ICatalogReadPersistenceGateway`), §8 (`/api/categories*` + `createCategorySchema`), §9, §15 (OpenAPI).
- [`../../CLAUDE.md`](../../CLAUDE.md) — padrão de use case, modelo de erros, Repository vs PersistenceGateway.
