# RF-12 — TikTok + Pedidos (atrás de interface)

> Subtask autocontida. Um agente deve conseguir implementar este RF lendo só este arquivo + as seções referenciadas da PRD/TECH_SPEC.

| | |
| --- | --- |
| **Tipo** | back-end (TikTok + Pedidos) |
| **Onda** | 1 |
| **Depende de** | RF-01, RF-03 (devem estar `DONE` em `progress-claude.txt`) |
| **Habilita** | RF-13 (Pedidos front), RF-14 (empacotamento back), RF-16 (relatórios-core back), RF-20 (adapter real TikTok — adiado) |
| **Hotspots tocados** | `src/server/di/container.ts`, `src/server/di/symbols.ts`, `src/lib/schemas.ts`, `src/lib/openapi.ts`, `src/lib/error-messages.ts` |

## 1. Objetivo (o que este RF entrega)

A loja passa a ver os **pedidos vindos do TikTok** (cliente, valor, frete, status), com os **pendentes no topo**, sem digitar nada à mão. Tecnicamente: as **portas** (interfaces) das integrações TikTok (Pedidos + Ads), os **ACL translators**, os **Fakes** que ligam o produto fim-a-fim sem a API real, a entidade/repo `Order` (espelho), os casos de uso `IngestTikTokOrderUseCase` (ingestão idempotente) e `ListOrdersUseCase` (leitura por período), a rota `GET /api/orders` e o **stub do webhook**.

## 2. Contexto (por quê)

Nesta fase os pedidos **deixam de ser criados à mão**: vêm do TikTok e alimentam a fila a empacotar (PRD F5, D14/D15). Como o **acesso à Partner API BR ainda não está confirmado** (TECH_SPEC §17, risco 🔴), tudo é construído **atrás de interfaces (ACL) + espelho no nosso banco** (TS9/TS10): o resto do app só lê do **nosso** banco. O **adapter real do TikTok + webhook real é o único item adiado** (RF-20) — aqui entregamos apenas as **portas + ACL + Fakes**, suficientes para ligar o produto de ponta a ponta em dev/homologação. A entidade `Order` é o **espelho** (mirror) do pedido do TikTok; o cliente é identificado por **número do pedido + nome do destinatário** (TS11), pois a API não fornece @username.

## 3. Início — pré-condições (o que já precisa existir)

- **RF-01 `DONE`:** `prisma/schema.prisma` migrado com `Order`, `Packing`, enums `ShipmentStatus`/`PackingStatus` (§4); VOs `Money`/`Period`; `DomainError` + `toHttpResponse`; container DI back + `symbols.ts`; banco de teste + helpers (`givenOrder`).
- **RF-03 `DONE`:** `src/lib/schemas.ts` (com `periodQuerySchema` + convenção de DTO `z.input`), `src/lib/openapi.ts`, rotas `/api/openapi.json` e `/api/reference`.
- Como conferir: `bun run db:migrate` cria a tabela `orders`; `grep -rn "periodQuerySchema" src/lib/`.
- **Não altere `prisma/schema.prisma`** — ele já contempla todos os models (criado só pelo RF-01).

## 4. Escopo — entregáveis (faça exatamente isto)

**Portas (interfaces — `src/server/application/gateway/`):**

- `ITikTokOrdersGateway.ts` — `searchOrders(period: Period): Promise<TikTokOrderDTO[]>` (backfill) + `getOrder(tiktokOrderId): Promise<TikTokOrderDTO>` (detalhe).
- `ITikTokAdsGateway.ts` — `getSpend(period: Period): Promise<{ amountCents: number } | { unavailable: true }>`.
- `IOrderListPersistenceGateway.ts` — read gateway: lista `OrderListItem[]` do período (pendentes no topo, ordenados por data do pedido).

**Repositório do agregado `Order` (§7):**

- `src/server/domain/repository/IOrderRepository.ts` — contrato do agregado (camada de domínio): `upsert(order)` (idempotente pelo `tiktokOrderId`), `findById`/`findByTiktokOrderId`.
- `src/server/infrastructure/repository/OrderPrismaRepository.ts` — implementa `IOrderRepository`; `mapToEntity`/`mapToPersistence` **privados, no fim da classe**; lança `ORDER_NOT_FOUND` (404) quando busca por id não encontra.

**Entidade de domínio + ACL:**

- `src/server/domain/entity/Order.ts` — entidade rica (espelho do TikTok). `restore({...})`; comportamentos `markPacked()`, `markNotPacked()`, `applyShipmentStatus(status)`; regra `canBePacked()` (`CANCELLED` não empacota → `OrderCannotBePackedError`). Getters de venda/frete como `Money`.
- `src/server/domain/acl/TikTokOrderTranslator.ts` — `toDomain(dto): Order` (mapeia status TikTok → `ShipmentStatus`, payment → `Money`) e `toDTO(order): TikTokOrderDTO`.
- `src/server/domain/acl/TikTokAdsTranslator.ts` — tradução do gasto de ads (resposta → centavos).

**Fakes (apoio — `test/fakes/`, NÃO testados):**

- `test/fakes/FakeTikTokOrdersGateway.ts` — implementa `ITikTokOrdersGateway` com dados realistas em memória (derivados do seed atual do front).
- `test/fakes/FakeTikTokAdsGateway.ts` — implementa `ITikTokAdsGateway` em memória.

**Casos de uso** (`src/server/application/use-case/` + contrato em `.../contracts/`, padrão §6.1):

- `IngestTikTokOrderUseCase` — `{ tiktokOrderDTO }` → Order (usa `TikTokOrderTranslator.toDomain()` → `OrderRepository.upsert()`; **cria** quando novo, **atualiza** status/dados quando já existe — upsert idempotente pelo `tiktokOrderId`). Sem erros de regra.
- `ListOrdersUseCase` (DTO) — `period + status?` → `OrderListItem[]` (read gateway; **pendentes no topo**, ordenados por data do pedido). Erro: validação de período.

**Rotas** (`src/app/api/`, borda HTTP: Zod + Clerk + `toHttpResponse`):

- `src/app/api/orders/route.ts` — `GET` (lista pedidos do período; `periodQuery + status?`). Resposta `OrderListItem[]`.
- `src/app/api/webhooks/tiktok/orders/route.ts` — `POST` **stub** do webhook (sem Clerk; HMAC do TikTok validado no adapter real do RF-20). Aqui só recebe o payload e **chama `IngestTikTokOrderUseCase`**; responde `200`.

**Schemas/DTOs/OpenAPI/erros (aditivo):**

- `src/lib/schemas.ts` — `listOrdersSchema` (= `periodQuerySchema` + `status?`) → `ListOrdersDTO`. DTO de leitura `OrderListItem` (read model, não-entidade).
- `src/lib/openapi.ts` — registrar `GET /api/orders` (+ webhook) e respostas de erro.
- `src/lib/error-messages.ts` — entradas PT para `ORDER_NOT_FOUND`, `ORDER_CANNOT_BE_PACKED`, `TIKTOK_NOT_CONFIGURED`, `TIKTOK_API_ERROR`.
- `src/server/di/container.ts` + `symbols.ts` — bindings de `OrderRepository`, `OrderListPersistenceGateway`, `ITikTokOrdersGateway`/`ITikTokAdsGateway` (ligados aos **Fakes** nesta fase), translators e os 2 use cases.

### Fora do escopo (NÃO faça)

- **Não** implementar os HttpGateways reais `TikTokOrdersHttpGateway`/`TikTokAdsHttpGateway` (base `TikTokHttpGateway`), OAuth, assinatura HMAC, webhook real ou sync/backfill agendado — isso é **RF-20 (adiado)**. Aqui só portas + ACL + Fakes.
- **Não** criar o agregado `Packing` nem os use cases de empacotamento (`GetOrderForPacking`/`SavePacking`/`DeletePacking`) — RF-14.
- **Não** criar `IAdSpendPersistenceGateway`/`GetAdSpendUseCase`/`SetManualAdSpendUseCase` nem as rotas `/api/ad-spend*` — RF-16 (a porta `ITikTokAdsGateway` + Fake já entram aqui para reuso).
- **Não** criar telas/front — RF-13.
- **Não** alterar `prisma/schema.prisma`.

## 5. Fim — Definition of Done

- Lista de pedidos vinda do **Fake**, com **pendentes no topo** ordenados por data do pedido (mais antigo primeiro), em fuso SP.
- `IngestTikTokOrderUseCase` **idempotente**: cria quando novo, atualiza quando existe (upsert pelo `tiktokOrderId`).
- `Order.canBePacked()` é falso para `CANCELLED`; `markPacked`/`markNotPacked` alternam estado; mapeamento de status TikTok → `ShipmentStatus` correto.
- Portas `ITikTokOrdersGateway`/`ITikTokAdsGateway` ligadas aos Fakes no container; o resto do app só lê do nosso banco.
- Erros cobertos: `ORDER_NOT_FOUND` (404), `ORDER_CANNOT_BE_PACKED` (422), `VALIDATION_ERROR` (400); códigos `TIKTOK_NOT_CONFIGURED`/`TIKTOK_API_ERROR` (502) registrados para o RF-20.
- DTO reusado como `Input` do contrato de `ListOrdersUseCase` (§6.1).
- **Testes da §14.2 passando** (`bun test`): integração `IngestTikTokOrderUseCase` e `ListOrdersUseCase` + unidade `Order`.
- `bun run lint` limpo; sem `any`.

## 6. Testes que cobrem este RF (§14.2)

Integração (`test/application/*.test.ts`, banco de teste + fakes):

- **`IngestTikTokOrderUseCase`** — **cria** o pedido espelhado quando novo e **atualiza status/dados** quando já existe (upsert idempotente pelo `tiktokOrderId`).
- **`ListOrdersUseCase`** — retorna os pedidos do período (limites em fuso SP), com **pendentes no topo** e ordenados por data do pedido (mais antigo primeiro).

Unidade (`test/domain/*.spec.ts`):

- **`Order`** — `canBePacked()` é falso para `CANCELLED`; `markPacked`/`markNotPacked` alternam o estado; mapeamento de status do TikTok correto.

> Os Fakes e os repos Prisma **não** têm teste próprio (§14: infra e fakes não são alvo); os repos são exercitados **indiretamente** pelos testes de caso de uso.

## 7. Trecho da PRD que fala deste RF

> ### F5 — Integração com o TikTok (Pedidos + Ads)
>
> **História:** _Como dono, quero que os pedidos e o gasto com anúncios venham automaticamente do TikTok, para o operador não digitar cliente/valor/frete e o lucro refletir o custo real de mídia._
>
> **Requisitos:**
>
> - Conectar a conta do TikTok da loja (fluxo de conexão da integração).
> - Puxar, por período, **os pedidos** com: id do pedido, @cliente, valor da venda, frete, data/hora do pedido e **status de envio** (pendente/enviado). Alimenta a página Pedidos (F2) e o Histórico (F3).
> - Puxar, por período, o **total gasto com Ads**. Alimenta o CPA (F3) e o lucro (F3/F4).

> **Regras de negócio:**
>
> - **RN-5.1 (pedidos automáticos):** cliente, valor da venda, frete e status vêm do TikTok; o operador **não digita** nem cria pedidos manualmente.
> - **RN-5.5 (resiliência):** falha da integração **nunca** quebra as telas — o que já foi sincronizado continua visível, com aviso amigável.

> #### F2a — Página "Pedidos" (lista)
>
> **Regras de negócio:**
>
> - **RN-2.1 (origem dos pedidos):** a lista é alimentada **exclusivamente** pela integração TikTok (F5); não há criação manual de pedido.
> - **RN-2.2 (ordenação padrão):** pendentes primeiro, ordenados pela data/hora do pedido (mais antigo no topo, para empacotar na ordem da fila).

> | D9  | **Sem conceito de "live"**; pedidos são organizados por data/hora (período).                                                                                                               |
> | D14 | **Pedidos vêm do TikTok** (cliente, valor, frete, status de envio). **Sem criação manual** de pedido nesta fase.                                                                           |
> | D15 | A página "Empacotar" vira **"Pedidos"**: lista por período (lógica do Dashboard), **pendentes no topo** em ordem de pedido; "Empacotar" abre a tela com o **id do pedido na URL**.         |

## 8. Trecho da TECH_SPEC que fala deste RF

Decisões (§1):

> | TS9  | **TikTok atrás de interface (ACL) + espelho no nosso banco.** Tudo é construído com um **Fake adapter**; o **adapter real do TikTok + webhook é o único item adiado** até confirmar acesso à API BR. |
> | TS10 | Sincronização de pedidos: **webhook `ORDER_STATUS_UPDATE` + backfill via `orders/search`**, espelhado no nosso banco. O resto do app só lê do nosso banco. |
> | TS11 | Identificação do pedido: **número do pedido (TikTok) + nome do destinatário** (sem máscara se a API permitir; com máscara se for o que vier).          |

Modelo de dados (§4):

```prisma
enum ShipmentStatus {
    PENDING      // não enviado (AWAITING_SHIPMENT/AWAITING_COLLECTION/UNPAID no TikTok)
    SHIPPED      // IN_TRANSIT/DELIVERED/COMPLETED
    CANCELLED
}

enum PackingStatus {
    NOT_PACKED
    PACKED
}

model Order {
    id             String         @id @default(cuid())
    tiktokOrderId  String         @unique
    orderNumber    String
    recipientName  String?
    saleCents      Int
    shippingCents  Int            @default(0)
    orderedAt      DateTime
    shipmentStatus ShipmentStatus @default(PENDING)
    packingStatus  PackingStatus  @default(NOT_PACKED)  // campo interno nosso
    packing        Packing?
    createdAt      DateTime       @default(now())
    updatedAt      DateTime       @updatedAt

    @@index([orderedAt])
    @@index([shipmentStatus, packingStatus])
    @@map("orders")
}
```

Entidade `Order` (§5.2):

> **`Order`** (espelho do TikTok) — `restore({...})` a partir do espelho. Comportamentos internos: `markPacked()`, `markNotPacked()`, `applyShipmentStatus(status)`. Regra: `canBePacked()` (PRD: empacota pendentes; um pedido `CANCELLED` não empacota → `OrderCannotBePackedError`). Getters de venda/frete como `Money`.

ACL (§5.4):

```ts
// domain/acl/TikTokOrderTranslator.ts
export class TikTokOrderTranslator {
    toDomain(dto: TikTokOrderDTO): Order;        // mapeia status TikTok → ShipmentStatus, payment → Money
    toDTO(order: Order): TikTokOrderDTO;
}
```

Inventário de casos de uso (§6.2):

> | `IngestTikTokOrderUseCase`      | `{ tiktokOrderDTO }` → Order (espelha/atualiza; webhook + backfill)        | —                                                                                     |
> | `ListOrdersUseCase` (DTO)       | period+status → pedidos (pendentes no topo, read gateway)                  | (validação de período)                                                                |

Integração TikTok (§10) — **portas (§10.1)**:

```ts
// application/gateway/ITikTokOrdersGateway.ts
export interface ITikTokOrdersGateway {
    searchOrders(period: Period): Promise<TikTokOrderDTO[]>;   // backfill
    getOrder(tiktokOrderId: string): Promise<TikTokOrderDTO>;  // detalhe (se preciso)
}
// application/gateway/ITikTokAdsGateway.ts
export interface ITikTokAdsGateway {
    getSpend(period: Period): Promise<{ amountCents: number } | { unavailable: true }>;
}
```

> **Implementações (HttpGateway):** `TikTokOrdersHttpGateway` e `TikTokAdsHttpGateway` estendem a base **`TikTokHttpGateway`** e dependem da abstração **`HttpClient`** (§7.3) — nunca de `axios` direto. A base concentra o que é comum à integração (injeção do `HttpClient`, montagem de headers via `setHeaders`, assinatura HMAC dos requests do Shop, base URLs). A tradução resposta↔domínio é feita pela **ACL** (`TikTokOrderTranslator` / `TikTokAdsTranslator`). Os **Fakes** (`FakeTikTokOrdersGateway`/`FakeTikTokAdsGateway`) implementam as mesmas portas sem `HttpClient` (dados em memória).

Endpoints reais (§10.2) — **contexto para o RF-20 (adiado); não implemente aqui**:

> **Pedidos — TikTok Shop Partner API** (host `https://open-api.tiktokglobalshop.com`, versão `202309`):
>
> | Passo | Método | Caminho | Notas |
> | ----- | ------ | ------- | ----- |
> | Token | GET | `auth.tiktok-shops.com/api/v2/token/get` | `app_key`, `app_secret`, `auth_code`; usa expiry absoluto retornado |
> | Refresh | GET | `auth.tiktok-shops.com/api/v2/token/refresh` | `refresh_token` |
> | Shops | GET | `/authorization/202309/shops` | obtém `shop_cipher` (necessário nas chamadas) |
> | Buscar pedidos | **POST** | `/order/202309/orders/search` | filtros de tempo (`create_time_ge/lt`), `page_size≤100`, `page_token`; status |
> | Detalhe | GET | `/order/202309/orders?ids=...` | só se faltar campo no summary |
>
> - **Auth por request:** header `x-tts-access-token`, `shop_cipher` na query, `app_key`+`timestamp`, e **assinatura HMAC-SHA256** (`sign`).
> - **Campos:** `payment.total_amount` (venda), `payment.shipping_fee` (frete), `create_time` (data), `order_status`. **Sem @username** → usar `recipient_address` (nome, possivelmente mascarado) + número do pedido (TS11).
> - **Status → nosso `ShipmentStatus`:** `UNPAID|AWAITING_SHIPMENT|AWAITING_COLLECTION|PARTIALLY_SHIPPING` → `PENDING`; `IN_TRANSIT|DELIVERED|COMPLETED` → `SHIPPED`; `CANCELLED` → `CANCELLED`.
> - **Webhook:** `POST /api/webhooks/tiktok/orders` recebe `type=1 ORDER_STATUS_UPDATE`; verificar HMAC-SHA256 de `app_key + rawBody` no header `Authorization`; chamar `IngestTikTokOrderUseCase`.
>
> **Ads — TikTok Business/Marketing API** (host `https://business-api.tiktok.com/open_api`, `v1.3`):
>
> | Passo | Método | Caminho | Notas |
> | ----- | ------ | ------- | ----- |
> | Token | POST | `/v1.3/oauth2/access_token/` | `{app_id, secret, auth_code}` — **token não expira** |
> | Advertiser | GET | `/v1.3/oauth2/advertiser/get/` | obtém `advertiser_id` |
> | Gasto | GET | `/v1.3/report/integrated/get/` | `report_type=BASIC`, `data_level=AUCTION_ADVERTISER`, `dimensions=["advertiser_id"]`, `metrics=["spend"]`, `start_date`, `end_date` |
>
> - **Total do período:** sem dimensão de tempo, vem 1 linha agregada `data.list[0].metrics.spend`. Janela máx ~30 dias/request (paginar/fatiar).
> - **Sandbox Ads:** host `https://sandbox-ads.tiktok.com/open_api` (baixo risco). **Sandbox de Pedidos:** só UK/Indonésia (não há BR) — risco §17.

Espelho + sincronização (§10.3):

> - `IngestTikTokOrderUseCase` usa `TikTokOrderTranslator.toDomain()` → `OrderRepository.upsert()`. O webhook e o backfill (busca por período no primeiro carregamento / agendado) alimentam o espelho.
> - Ads: um job (ou no `GetAdSpendUseCase`) busca via `ITikTokAdsGateway`, grava `AdSpendDay(source=TIKTOK)`. Se `unavailable`, o front usa o valor `MANUAL` do dia (se houver) e marca `available:false`.

Fake adapter (§10.4):

> `FakeTikTokOrdersGateway` e `FakeTikTokAdsGateway` retornam dados realistas (derivados do seed atual do front). É o que liga o produto fim-a-fim sem a API real, e o que os testes de integração de casos de uso usam.

Repositórios e gateways (§7):

> | Repository          | `domain/repository/IOrderRepository`                    | `infrastructure/repository/OrderPrismaRepository`              |
> | PersistenceGateway  | `application/gateway/IOrderListPersistenceGateway`      | `infrastructure/gateway/OrderListPrismaPersistenceGateway`     |
> | External Gateway    | `application/gateway/ITikTokOrdersGateway`              | `infrastructure/gateway/TikTokOrdersHttpGateway` (fake: `test/fakes/FakeTikTokOrdersGateway`) |
> | External Gateway    | `application/gateway/ITikTokAdsGateway`                 | `infrastructure/gateway/TikTokAdsHttpGateway` (fake: `test/fakes/FakeTikTokAdsGateway`) |

> **Nomeação TikTok:** os dois HttpGateways (Pedidos via Shop API e Ads via Business API — hosts/auth distintos) estendem uma base comum **`TikTokHttpGateway`** que concentra `HttpClient` + assinatura/headers; `TikTokOrdersHttpGateway` e `TikTokAdsHttpGateway` herdam dela. Assim respeitamos a convenção `<Integração>HttpGateway` e o ISP das portas.

Endpoints (§8):

> | GET    | `/api/orders`                        | Lista pedidos do período (pendentes no topo)                    | `periodQuery + status?`                         | `OrderListItem[]`                         |
> | POST   | `/api/webhooks/tiktok/orders`        | Recebe `ORDER_STATUS_UPDATE` (sem Clerk; HMAC do TikTok)        | payload TikTok                                  | `200`                                     |

DTO de leitura (§8):

```ts
type OrderListItem = {
    orderId: string; orderNumber: string; recipientName: string | null;
    saleCents: number; shippingCents: number; orderedAt: string;
    shipmentStatus: "PENDING" | "SHIPPED" | "CANCELLED";
    packingStatus: "NOT_PACKED" | "PACKED";
};
```

Erros (§9):

> | `ORDER_NOT_FOUND`              | 404  | repo              |
> | `ORDER_CANNOT_BE_PACKED`       | 422  | domínio           |
> | `TIKTOK_NOT_CONFIGURED`        | 502  | gateway externo   |
> | `TIKTOK_API_ERROR`            | 502  | gateway externo   |

Linha do catálogo (§18.2):

> | **RF-12** | TikTok + Pedidos (atrás de interface): portas `ITikTokOrdersGateway`/`ITikTokAdsGateway` + ACL translators + **Fakes** + entidade/repo `Order` + `IngestTikTokOrderUseCase` + `ListOrdersUseCase` (+ read gateway) + rota `GET /api/orders` + stub do webhook (usa o use case). | RF-01, RF-03 | schema migrado | lista de pedidos vinda do Fake, pendentes no topo; ingestão idempotente. **Testes:** integração `IngestTikTokOrderUseCase` e `ListOrdersUseCase` + unidade `Order` (§14.2). |

## 9. Referências

- PRD: [`../PRD.md`](../PRD.md) — F5 (RN-5.1, RN-5.5), F2a (RN-2.1, RN-2.2), §3 (Pedido/Dados do TikTok), D9, D14, D15.
- TECH_SPEC: [`../TECH_SPEC.md`](../TECH_SPEC.md) — §10 (integração TikTok completa: §10.1 portas, §10.2 endpoints reais, §10.3 espelho/sync, §10.4 Fakes), §5.2 (`Order`), §5.4 (ACL), §4 (model `Order` + enums), §6.2 (inventário), §7 (repo/gateway), §8 (`/api/orders` + webhook + `OrderListItem`), §9 (erros), §15 (OpenAPI), TS9/TS10/TS11.
- [`../../CLAUDE.md`](../../CLAUDE.md) — padrão de use case, ACL + `mapToEntity`/`mapToPersistence`, Repository vs PersistenceGateway, modelo de erros.
- **RF-20 (adiado):** o adapter real do TikTok (HttpGateways + OAuth + HMAC + webhook real) pluga atrás destas mesmas portas, sem tocar no resto.
