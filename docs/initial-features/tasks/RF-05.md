# RF-05 — Domínio do catálogo (Category & Tier)

> Subtask autocontida. Um agente deve conseguir implementar este RF lendo só este arquivo + as seções referenciadas da PRD/TECH_SPEC.

| | |
| --- | --- |
| **Tipo** | back-end (domínio catálogo) |
| **Onda** | 1 |
| **Depende de** | RF-01 (deve estar `DONE` em `progress-claude.txt`) |
| **Habilita** | RF-06 (use cases de categoria), RF-07 (use cases de faixa) |
| **Hotspots tocados** | `src/server/di/container.ts`, `src/server/di/symbols.ts` (bindings dos repos) |

## 1. Objetivo (o que este RF entrega)

As **entidades ricas** `Category` e `Tier` (com comportamento de domínio e validações) e os **repositórios** que as persistem/recuperam como agregados. Tecnicamente: contratos `ICategoryRepository`/`ITierRepository` no domínio, implementações Prisma na infraestrutura (`mapToEntity` privado) e os bindings de DI — a base sobre a qual os casos de uso de catálogo (RF-06/07) operam.

## 2. Contexto (por quê)

O sortimento da loja muda toda semana. A virada de chave do produto é **não rastrear qual item, e sim a faixa de custo** (ex.: "Caneta R$1"). `Category` agrupa faixas só para organizar a tela; `Tier` representa um preço de compra com código de barras único gerado pelo sistema. Estas entidades são o coração do catálogo e precisam carregar **comportamento** (renomear valida nome, custo deve ser > 0), nunca serem modelos anêmicos.

## 3. Início — pré-condições (o que já precisa existir)

- **RF-01 `DONE`:** `prisma/schema.prisma` migrado com os models `Category` e `Tier` (§4); VOs `Money`/`Barcode` em `src/server/domain/value-object/`; `DomainError` + subclasses (`CategoryNameRequiredError`, `TierCostMustBePositiveError`); container DI back + `symbols.ts`; banco de teste + helpers (`givenTier`).
- Como conferir: `bun run db:migrate` cria as tabelas `categories` e `tiers`; `grep -rn "class Money" src/server/domain/value-object/`.
- **Não altere `prisma/schema.prisma`** — ele já contempla todos os models (criado só pelo RF-01).

## 4. Escopo — entregáveis (faça exatamente isto)

- `src/server/domain/entity/Category.ts` — entidade rica. `create(name)`, `rename(name)` (nome obrigatório → `CategoryNameRequiredError`); `create()` valida invariantes, `restore()` reconstitui a partir do banco. Propriedades privadas, sem setters.
- `src/server/domain/entity/Tier.ts` — entidade rica. `create({ name, cost: Money, barcode, categoryId })`; `rename(name)`; `changeCost(Money)` (>0 → `TierCostMustBePositiveError`); `assignToCategory(id)` / `moveToUncategorized()`. Getters: `getBarcode()`, `getCost()`, `getCategoryId()`. `create()`/`restore()`.
- `src/server/domain/repository/ICategoryRepository.ts` — contrato do agregado (camada de domínio).
- `src/server/domain/repository/ITierRepository.ts` — contrato do agregado (camada de domínio).
- `src/server/infrastructure/repository/CategoryPrismaRepository.ts` — implementa `ICategoryRepository`; `mapToEntity` (e `mapToPersistence` se preciso) **privados, no fim da classe**. Lança `CATEGORY_NOT_FOUND` (404) quando busca por id não encontra.
- `src/server/infrastructure/repository/TierPrismaRepository.ts` — implementa `ITierRepository`; `mapToEntity` privado no fim; lança `TIER_NOT_FOUND` (404) quando não encontra; mapeia violação de `@unique` do barcode para `DUPLICATE_BARCODE` (422).
- `src/server/di/container.ts` + `src/server/di/symbols.ts` — **acrescentar** (de forma aditiva) os símbolos e bindings `CategoryRepository`/`TierRepository`.

### Fora do escopo (NÃO faça)

- Não criar **casos de uso** (`CreateCategoryUseCase` etc.) — RF-06/07.
- Não criar `BarcodeCodeGenerator` nem a geração do código — RF-07.
- Não criar `ICatalogReadPersistenceGateway`/read model — RF-06.
- Não criar rotas, schemas Zod ou OpenAPI — RF-06/07.
- Não alterar `prisma/schema.prisma`.

## 5. Fim — Definition of Done

- Repos persistem e recuperam os agregados `Category`/`Tier` corretamente (via `mapToEntity`).
- Entidades carregam comportamento: `Category.rename` e `Tier.rename` validam nome; `Tier.changeCost` rejeita ≤ 0; `assignToCategory`/`moveToUncategorized` mudam a categoria.
- Bindings de DI resolvem `ICategoryRepository`/`ITierRepository`.
- **Testes da §14.2 passando** (`bun test`): unidade de `Category` e `Tier`.
- `bun run lint` limpo; sem `any`.

## 6. Testes que cobrem este RF (§14.2)

Unidade (`test/domain/*.spec.ts`):

- **`Category`** — `rename` valida nome (erro de domínio em nome vazio).
- **`Tier`** — `changeCost` rejeita ≤ 0; `rename` valida; `moveToUncategorized`/`assignToCategory` mudam a categoria.

> Repos Prisma **não** têm teste próprio (§14: infra não é alvo); são exercitados **indiretamente** pelos testes de caso de uso (RF-06/07).

## 7. Trecho da PRD que fala deste RF

> ### F1 — Categorias e faixas de custo (CRUD)
>
> **Regras de negócio:**
>
> - **RN-1.4** Custo de faixa deve ser **maior que zero**.
> - **RN-1.5** Nome de categoria e de faixa são obrigatórios (não vazios).

Do modelo de domínio (§3):

> | **Categoria**                       | Agrupamento livre de faixas, só para organizar a tela (ex.: Canetas, Cadernos, Estojos). Criada/renomeada/excluída sob demanda.             | nome; data de criação                                                                                                                                                                            |
> | **Faixa de custo (tier)**           | Rótulo que representa um **preço de compra** (ex.: "Caneta R$1", "Caderno R$8"). Marca/cor/personagem não importam — só o que foi pago.     | nome; custo (R$); **código de barras gerado pelo sistema**; categoria a que pertence                                                                                                             |

## 8. Trecho da TECH_SPEC que fala deste RF

Modelo de dados (§4):

```prisma
model Category {
    id        String   @id @default(cuid())
    name      String
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
    tiers     Tier[]

    @@map("categories")
}

model Tier {
    id         String    @id @default(cuid())
    name       String
    costCents  Int
    barcode    String    @unique
    categoryId String?                       // null = "Sem categoria"
    category   Category? @relation(fields: [categoryId], references: [id], onDelete: SetNull)
    createdAt  DateTime  @default(now())
    updatedAt  DateTime  @updatedAt

    @@index([categoryId])
    @@map("tiers")
}
```

Domínio — entidades ricas (§5 intro):

> Entidades **ricas** (CLAUDE.md): propriedades privadas, sem setters; métodos com intenção de domínio; validações lançam erros de domínio. Cada entidade tem `create()` (nova, valida invariantes) e `restore()` (reconstituição a partir do banco).

Entidades (§5.2):

> **`Category`** — `create(name)`, `rename(name)` (nome obrigatório → `CategoryNameRequiredError`).
>
> **`Tier`** — `create({ name, cost: Money, barcode, categoryId })`; `rename(name)`; `changeCost(Money)` (>0 → `TierCostMustBePositiveError`); `assignToCategory(id)` / `moveToUncategorized()`. Getters: `getBarcode()`, `getCost()`, `getCategoryId()`.

Convenção de repositórios (§7):

> - **Repository** = serviço de domínio sobre um **agregado**. Contrato em `domain/repository/I<Aggregate>Repository.ts`. Implementação em `infrastructure/repository/<Aggregate>PrismaRepository.ts`. Persiste o **agregado inteiro**; tem `mapToEntity` (e `mapToPersistence` se preciso) **privados, no fim da classe**.

> | Repository          | `domain/repository/ICategoryRepository`                 | `infrastructure/repository/CategoryPrismaRepository`           |
> | Repository          | `domain/repository/ITierRepository`                     | `infrastructure/repository/TierPrismaRepository`               |

Linha do catálogo (§18.2):

> | **RF-05** | Domínio catálogo: entidades `Category` e `Tier` + contratos `ICategoryRepository`/`ITierRepository` + impls Prisma. | RF-01 | schema migrado | repos persistem/recuperam agregados (mapToEntity). **Testes:** unidade `Category` e `Tier` (§14.2). |

## 9. Referências

- PRD: [`../PRD.md`](../PRD.md) — F1 (RN-1.4, RN-1.5), §3 (modelo de domínio).
- TECH_SPEC: [`../TECH_SPEC.md`](../TECH_SPEC.md) — §4 (models Category/Tier), §5 intro, §5.2, §7 (Repository convention).
- [`../../CLAUDE.md`](../../CLAUDE.md) — regras de entidades ricas, Repository vs PersistenceGateway.
