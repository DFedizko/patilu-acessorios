# RF-14 — Empacotamento (agregado Packing + use cases)

> Subtask autocontida. Um agente deve conseguir implementar este RF lendo só este arquivo + as seções referenciadas da PRD/TECH_SPEC.

| | |
| --- | --- |
| **Tipo** | back-end (empacotamento) |
| **Onda** | 2 |
| **Depende de** | RF-12, RF-05 (devem estar `DONE` em `progress-claude.txt`) — RF-05 fornece o `ITierRepository` que o `SavePackingUseCase` usa para congelar nome/categoria/custo da faixa |
| **Habilita** | RF-15 (empacotamento front), RF-17 (Histórico back), RF-18 (Dashboard back) |
| **Hotspots tocados** | `src/server/di/container.ts`, `src/server/di/symbols.ts`, `src/lib/schemas.ts`, `src/lib/openapi.ts`, `src/lib/error-messages.ts` |

## 1. Objetivo (o que este RF entrega)

O **empacotamento de um pedido**: o operador conta as faixas e os itens avulsos que entraram na caixa, vê o custo total e o sistema **congela o snapshot** (custo/nome de faixa/categoria) ao concluir, marcando o pedido como `PACKED` e registrando quem empacotou. Tecnicamente: o agregado rico `Packing` (com `PackingItem`/`LooseItem`), o `PackingPrismaRepository`, os 3 casos de uso (`GetOrderForPackingUseCase`, `SavePackingUseCase`, `DeletePackingUseCase`), as rotas `/api/orders/:id/packing` e schema/DTO/OpenAPI.

## 2. Contexto (por quê)

É o **coração do produto**: aqui se calcula o custo do kit e a margem ao vivo. A virada do modelo é **contar a faixa de custo**, não o item. O empacotamento **congela** o detalhamento (RN-2.8 / D3): cada `PackingItem` guarda o custo unitário, o nome da faixa e o nome da categoria **da época**, para que editar/excluir uma faixa depois **nunca** quebre o histórico (RN-3.5 / D4). A margem **ao vivo** (RN-2.5) é só venda − custo − frete (sem CPA/custo fixo, que são de período — RF-16/17/18). Um empacotamento pode ser **editado e excluído** (excluir devolve o pedido a pendente — RN-3.4 / D5); cliente/valor/frete vêm do TikTok e **não** se editam aqui (D17).

## 3. Início — pré-condições (o que já precisa existir)

- **RF-12 `DONE`:** entidade/repo `Order` (espelho) + `IOrderRepository`; `IngestTikTokOrderUseCase`/`ListOrdersUseCase`; rota `GET /api/orders`; enums `ShipmentStatus`/`PackingStatus`; `Order.canBePacked()`/`markPacked()`/`markNotPacked()`.
- **RF-01 `DONE`:** models `Packing`/`PackingItem`/`LooseItem` migrados (§4); VO `Money`; `DomainError` + `toHttpResponse`; container DI back + `symbols.ts`; banco de teste + helpers (`givenOrder`/`givenTier`).
- **RF-05/07 (catálogo) `DONE`** o suficiente para `ITierRepository` resolver uma faixa por `tierId` (o `SavePackingUseCase` lê a faixa para congelar nome/categoria/custo).
- Como conferir: container resolve `OrderRepository` e `TierRepository`; `GET /api/orders` lista pedidos do Fake.
- **Não altere `prisma/schema.prisma`** — ele já contempla todos os models (criado só pelo RF-01).

## 4. Escopo — entregáveis (faça exatamente isto)

**Agregado de domínio (`src/server/domain/entity/`):**

- `Packing.ts` — agregado raiz; contém `PackingItem[]` e `LooseItem[]`. `create(orderId, operatorId)` / `restore(props)`. Intenções: `addTier(tier)` (+1, congela nome/categoria/custo), `incrementTier(tierId)`, `decrementTier(tierId)` (remove o item ao zerar), `addLooseItem(name, cost)`, `removeLooseItem(looseItemId)`. Cálculos: `computeItemsCost()` (RN-2.6), `computeLiveMargin(sale, shipping)` (RN-2.5 — R$ e %), `ensureCanConclude()` (≥1 item → senão `PackingRequiresItemError`, RN-2.7).
- `PackingItem.ts` / `LooseItem.ts` — entidades-filhas (snapshot congelado: `tierId?`, `tierName`, `categoryName`, `unitCostCents`, `quantity`; avulso: `name`, `costCents`).

**Repositório do agregado (§7):**

- `src/server/domain/repository/IPackingRepository.ts` — contrato (camada de domínio): salvar/carregar/excluir o **agregado inteiro** (com itens e avulsos), `findByOrderId`.
- `src/server/infrastructure/repository/PackingPrismaRepository.ts` — implementa `IPackingRepository`; persiste itens + avulsos juntos; `mapToEntity`/`mapToPersistence` **privados, no fim da classe**; lança `PACKING_NOT_FOUND` (404) quando não há empacotamento.

**Casos de uso** (`src/server/application/use-case/` + contrato em `.../contracts/`, padrão §6.1):

- `GetOrderForPackingUseCase` — `{ orderId }` → `{ order (read-only), packing? }`. Erro: `ORDER_NOT_FOUND`.
- `SavePackingUseCase` (DTO+) — `{ orderId, operatorId, items[], looseItems[] }` → Packing (cria/edita o snapshot, marca o pedido `PACKED`, registra `operatorId`; re-salvar **regrava** o snapshot). `orderId` da rota, `operatorId` do Clerk. Erros: `ORDER_NOT_FOUND`, `ORDER_CANNOT_BE_PACKED`, `PACKING_REQUIRES_ITEM`, `TIER_NOT_FOUND`.
- `DeletePackingUseCase` — `{ orderId }` → void (pedido volta a `NOT_PACKED`). Erro: `PACKING_NOT_FOUND`.

**Rotas** (`src/app/api/orders/[id]/packing/route.ts`, borda HTTP: Zod + Clerk + `toHttpResponse`):

- `GET` — pedido (read-only) + empacotamento atual → `{ order, packing? }`.
- `PUT` — cria/edita empacotamento; marca PACKED. Body `SavePackingInput`. `operatorId` vem do `userId` do Clerk (não do body).
- `DELETE` — remove empacotamento; pedido volta a pendente → `204`.

**Schemas/DTOs/OpenAPI/erros (aditivo):**

- `src/lib/schemas.ts` — `savePackingSchema` → `SavePackingDTO` (com `.refine` exigindo ≥1 item). `Input` do `SavePackingUseCase` = `SavePackingDTO & { orderId: string; operatorId: string }` (DTO+, §6.1).
- `src/lib/openapi.ts` — registrar `GET/PUT/DELETE /api/orders/:id/packing` + respostas de erro.
- `src/lib/error-messages.ts` — entradas PT para `PACKING_REQUIRES_ITEM`, `PACKING_NOT_FOUND` (e reuso de `ORDER_NOT_FOUND`/`ORDER_CANNOT_BE_PACKED`/`TIER_NOT_FOUND` se ainda não existirem).
- `src/server/di/container.ts` + `symbols.ts` — bindings de `PackingRepository` + os 3 use cases.

### Fora do escopo (NÃO faça)

- **Não** criar telas/front (cabeçalho read-only, painel de margem, bipe) — RF-15.
- **Não** criar `PeriodReportCalculator`, CPA, custo fixo ou ad spend — RF-16/17/18 (a margem **ao vivo** aqui é só venda − custo − frete, sem CPA/custo fixo).
- **Não** tocar na ingestão/listagem de pedidos — RF-12.
- **Não** alterar `prisma/schema.prisma`.

## 5. Fim — Definition of Done

- `SavePackingUseCase` **congela** custo/nome de faixa/categoria dos itens; calcula o custo total certo; marca o pedido `PACKED` e registra `operatorId`; **exige ≥1 item** (`PACKING_REQUIRES_ITEM`); pedido `CANCELLED` **não** empacota (`ORDER_CANNOT_BE_PACKED`); re-salvar **regrava** o snapshot.
- `DeletePackingUseCase` remove o empacotamento e o pedido volta a `NOT_PACKED`; sem empacotamento → `PACKING_NOT_FOUND`.
- `GetOrderForPackingUseCase` devolve o pedido read-only + empacotamento atual (se houver); pedido inexistente → `ORDER_NOT_FOUND`.
- A margem ao vivo (`computeLiveMargin`) = venda − custo − frete (R$ e %), **sem** CPA/custo fixo.
- Erros cobertos: `ORDER_NOT_FOUND` (404), `PACKING_NOT_FOUND` (404), `ORDER_CANNOT_BE_PACKED` (422), `PACKING_REQUIRES_ITEM` (422), `TIER_NOT_FOUND` (404), `VALIDATION_ERROR` (400).
- DTO reusado como base do `Input` do `SavePackingUseCase` (DTO+, §6.1).
- **Testes da §14.2 passando** (`bun test`): integração dos 3 casos de uso + unidade `Packing`.
- `bun run lint` limpo; sem `any`.

## 6. Testes que cobrem este RF (§14.2)

Integração (`test/application/*.test.ts`, banco de teste + fakes):

- **`GetOrderForPackingUseCase`** — retorna o pedido (read-only) + o empacotamento atual se houver; **erro** `ORDER_NOT_FOUND`.
- **`SavePackingUseCase`** — **congela** custo/nome de faixa/categoria dos itens; calcula o custo total certo; marca o pedido **`PACKED`** e registra `operatorId`; **exige ≥1 item** (`PACKING_REQUIRES_ITEM`); pedido `CANCELLED` **não** empacota (`ORDER_CANNOT_BE_PACKED`); re-salvar **regrava** o snapshot.
- **`DeletePackingUseCase`** — remove o empacotamento e o pedido **volta a `NOT_PACKED`**; **erro** `PACKING_NOT_FOUND` se não havia.

Unidade (`test/domain/*.spec.ts`):

- **`Packing`** — `computeItemsCost` = Σ(qtd × custo) + avulsos; `incrementTier`/`decrementTier` (some o item ao zerar); `computeLiveMargin` = venda − custo − frete (R$ e %, **sem** CPA/custo fixo); `ensureCanConclude` exige ≥1 item.

> Os repos Prisma **não** têm teste próprio (§14: infra não é alvo); são exercitados **indiretamente** pelos testes de caso de uso.

## 7. Trecho da PRD que fala deste RF

> #### F2b — Tela de empacotamento (de um pedido)
>
> **Regras de negócio:**
>
> - **RN-2.5 (margem ao vivo):** na tela de empacotamento, `margem R$ = valor da venda − custo dos itens − frete`. `margem % = (margem R$ ÷ valor da venda) × 100`. **Ao vivo não entram Ads/CPA nem custo fixo** — esses são de período e aparecem no Histórico/Dashboard (F3/F4). A margem ao vivo é o termômetro rápido "o conteúdo do kit cabe no preço?".
> - **RN-2.6 (custo dos itens):** soma de (quantidade × custo da faixa) de todas as faixas contadas + soma dos custos dos itens avulsos.
> - **RN-2.7 (concluir):** só é possível concluir com **pelo menos 1 item** (faixa contada ou avulso). O valor da venda já vem do TikTok (sempre presente). Sem itens, mensagem amigável impede a conclusão.
> - **RN-2.8 (snapshot):** ao concluir, o empacotamento **congela** o custo de cada item (faixa e avulso), o detalhamento (faixa + quantidade), o **nome da faixa/categoria da época**, e registra **operador, data e hora**.
> - **RN-2.10 (re-empacotar):** abrir "Editar empacotamento" de um pedido já empacotado recarrega o detalhamento; ao salvar, recalcula e regrava o snapshot.

> ### F3 — Histórico (por período)
>
> **Regras de negócio:**
>
> - **RN-3.4 (editar/excluir empacotamento):** é possível **editar** o empacotamento de um pedido (recontar itens/avulsos) e **excluir** o empacotamento (o pedido volta a "pendente / não empacotado"). Cliente, valor e frete **não** se editam aqui (vêm do TikTok). Ao editar, custo e margem são recalculados e o snapshot regravado.
> - **RN-3.5 (histórico congelado):** editar/excluir **faixas ou categorias** depois **não altera** pedidos já empacotados. Cada empacotamento mantém o custo, o nome da faixa e a categoria **da época**.

> | D3  | Cada **empacotamento** guarda o **detalhamento completo** (faixas + quantidades + avulsos), com custo **congelado** ao concluir.                                                           |
> | D4  | Histórico é **imutável a mudanças de catálogo**: editar/excluir faixa ou categoria não mexe em pedidos já empacotados.                                                                     |
> | D5  | O empacotamento de um pedido pode ser **editado e excluído** (excluir devolve o pedido a "pendente"). Cliente/valor/frete vêm do TikTok e **não** se editam.                               |

## 8. Trecho da TECH_SPEC que fala deste RF

Modelo de dados (§4) — snapshot congelado:

```prisma
model Packing {
    id         String        @id @default(cuid())
    orderId    String        @unique
    order      Order         @relation(fields: [orderId], references: [id], onDelete: Cascade)
    operatorId String                                    // Clerk userId
    packedAt   DateTime      @default(now())
    items      PackingItem[]
    looseItems LooseItem[]
    updatedAt  DateTime      @updatedAt

    @@map("packings")
}

// Snapshot congelado da faixa no momento do empacotamento (PRD RN-2.8 / RN-3.5)
model PackingItem {
    id           String  @id @default(cuid())
    packingId    String
    packing      Packing @relation(fields: [packingId], references: [id], onDelete: Cascade)
    tierId       String?                                 // referência fraca (faixa pode ser excluída depois)
    tierName     String                                  // congelado
    categoryName String                                  // congelado (para "custo por categoria")
    unitCostCents Int                                    // congelado
    quantity     Int

    @@index([packingId])
    @@map("packing_items")
}

model LooseItem {
    id        String  @id @default(cuid())
    packingId String
    packing   Packing @relation(fields: [packingId], references: [id], onDelete: Cascade)
    name      String
    costCents Int

    @@index([packingId])
    @@map("loose_items")
}
```

Agregado `Packing` (§5.2):

```ts
export class Packing {
    // create: novo empacotamento de um pedido
    static create(orderId: string, operatorId: string): Packing;
    static restore(props): Packing;

    // intenções de domínio
    addTier(tier: Tier): void;                 // +1 (toque/bipe) — congela nome/categoria/custo
    incrementTier(tierId: string): void;
    decrementTier(tierId: string): void;       // remove o item se chegar a 0
    addLooseItem(name: string, cost: Money): void;
    removeLooseItem(looseItemId: string): void;

    computeItemsCost(): Money;                  // Σ(qtd × custo) + Σ avulsos  (PRD RN-2.6)
    computeLiveMargin(sale: Money, shipping: Money): {
        marginValue: Money; marginPct: number;   // (PRD RN-2.5) — só R$ e %
    };
    ensureCanConclude(): void;                  // ≥1 item, senão PackingRequiresItemError (RN-2.7)
}
```

> A margem **ao vivo** (RN-2.5) **não** inclui CPA/custo fixo — esses são de período (Domain Service, §5.3).

Inventário de casos de uso (§6.2):

> | `GetOrderForPackingUseCase`     | `{ orderId }` → order(read-only) + packing atual (se houver)               | `ORDER_NOT_FOUND`                                                                     |
> | `SavePackingUseCase` (DTO+)     | `{ orderId, operatorId, items[], looseItems[] }` → Packing (cria/edita snapshot, marca PACKED) | `ORDER_NOT_FOUND`, `ORDER_CANNOT_BE_PACKED`, `PACKING_REQUIRES_ITEM`, `TIER_NOT_FOUND` |
> | `DeletePackingUseCase`          | `{ orderId }` → void (pedido volta a NOT_PACKED)                           | `PACKING_NOT_FOUND`                                                                   |

> Quando o caso de uso precisa de dados que **não** vêm do corpo validado (ex.: `id` da rota, `operatorId` do Clerk), o `Input` é o DTO do schema **acrescido** desses campos (ex.: `Input = SavePackingDTO & { orderId: string; operatorId: string }`); o schema do endpoint valida apenas o que vem do cliente.

Repositório (§7):

> | Repository          | `domain/repository/IPackingRepository`                  | `infrastructure/repository/PackingPrismaRepository`            |

Endpoints (§8):

> | GET    | `/api/orders/:id/packing`            | Pedido (read-only) + empacotamento atual                        | —                                               | `{ order, packing? }`                     |
> | PUT    | `/api/orders/:id/packing`            | Cria/edita empacotamento; marca PACKED                          | `SavePackingInput`                              | `Packing`                                 |
> | DELETE | `/api/orders/:id/packing`            | Remove empacotamento; pedido volta a pendente                   | —                                               | `204`                                     |

Schema Zod (§8):

```ts
export const savePackingSchema = z
    .object({
        items: z.array(z.object({ tierId: z.string().cuid2(), quantity: z.number().int().positive() })),
        looseItems: z.array(z.object({ name: z.string().trim().min(1), costReais: z.number().positive() })),
    })
    .refine((v) => v.items.length + v.looseItems.length > 0, { message: "Inclua ao menos um item" });
export type SavePackingDTO = z.input<typeof savePackingSchema>;
// SavePackingUseCase Input = SavePackingDTO & { orderId: string; operatorId: string }  (DTO+, §6.1)
```

Erros (§9):

> | `ORDER_NOT_FOUND`              | 404  | repo              |
> | `PACKING_NOT_FOUND`            | 404  | repo              |
> | `TIER_NOT_FOUND`               | 404  | repo              |
> | `PACKING_REQUIRES_ITEM`        | 422  | domínio           |
> | `ORDER_CANNOT_BE_PACKED`       | 422  | domínio           |

Linha do catálogo (§18.2):

> | **RF-14** | Empacotamento: entidade/repo `Packing` + `GetOrderForPackingUseCase`, `SavePackingUseCase` (snapshot + PACKED + operador + regras), `DeletePackingUseCase` + rotas `/api/orders/:id/packing` + Zod/OpenAPI. | RF-12 | `Order` + repos | endpoints de empacotamento funcionando; snapshot congelado. **Testes:** integração dos 3 casos de uso + unidade `Packing` (§14.2). |

## 9. Referências

- PRD: [`../PRD.md`](../PRD.md) — F2b (RN-2.5, RN-2.6, RN-2.7, RN-2.8, RN-2.10), F3 (RN-3.4, RN-3.5), D3, D4, D5, D17.
- TECH_SPEC: [`../TECH_SPEC.md`](../TECH_SPEC.md) — §5.2 (`Packing`), §4 (models `Packing`/`PackingItem`/`LooseItem`), §6.2 (inventário), §7 (`IPackingRepository`), §8 (`/api/orders/:id/packing` + `savePackingSchema`), §9 (erros), §15 (OpenAPI).
- [`../../CLAUDE.md`](../../CLAUDE.md) — padrão de use case (Input DTO+), entidades ricas, Repository, modelo de erros.
