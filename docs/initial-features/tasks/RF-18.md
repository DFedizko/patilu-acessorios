# RF-18 — Dashboard (indicadores + gráficos por período)

> Subtask autocontida. Um agente deve conseguir implementar este RF lendo só este arquivo + as seções referenciadas da PRD/TECH_SPEC.

| | |
| --- | --- |
| **Tipo** | back-end (Dashboard) |
| **Onda** | 3 |
| **Depende de** | RF-14, RF-16 (devem estar `DONE` em `progress-claude.txt`) |
| **Habilita** | RF-19 (front relatórios) |
| **Hotspots tocados** | `src/server/di/container.ts`, `src/server/di/symbols.ts`, `src/lib/schemas.ts`, `src/lib/openapi.ts` |

## 1. Objetivo (o que este RF entrega)

A leitura dos números do **Dashboard por período**: indicadores (receita, custo, lucro, margem média, nº de kits) **já com ads e custo fixo descontados**, a **série de margem ao longo do tempo** e o **custo por categoria**. Tecnicamente: o `GetDashboardUseCase` (usa o `PeriodReportCalculator` do RF-16) e a rota `GET /api/dashboard`, devolvendo o read model `DashboardData`.

## 2. Contexto (por quê)

O dono quer ver a performance do negócio por período para entender **lucro, margem e onde está o custo**. O Dashboard usa as **mesmas fórmulas** do Histórico (RN-4.1 = RN-3.3): lucro = receita − custo dos itens − frete − total de ads − (custo fixo × nº de pedidos). Além dos indicadores, mostra dois gráficos: **margem ao longo do tempo** (por hora no dia; por dia em períodos maiores) e **custo por categoria** (agrupado pela categoria **congelada** dos itens dos empacotamentos — RN-4.2). É um RF de leitura: monta `OrderInPeriod[]`, delega o cálculo ao `PeriodReportCalculator` e serializa em `DashboardData`.

## 3. Início — pré-condições (o que já precisa existir)

- **RF-14 `DONE`:** entidade/repo `Packing` com snapshot congelado (`categoryName`, `unitCostCents`, `quantity` em `PackingItem`); pedidos podem estar `PACKED`. O custo por categoria depende desse detalhamento congelado.
- **RF-16 `DONE`:** `PeriodReportCalculator` (`computePeriodProfit`, `computeCostByCategory`, `computeMarginSeries`) e o ad spend por período (`GetAdSpendUseCase`/`IAdSpendPersistenceGateway`); custo fixo via RF-10 (`GetFixedCostUseCase`/`IConfigPersistenceGateway`).
- **Decisão de reúso do read gateway:** o `GetDashboardUseCase` monta `OrderInPeriod[]` do período. Se o **RF-17 já estiver `DONE`**, **reuse a porta `IReportPersistenceGateway`** (mesmo read model do Histórico) — não duplique a leitura. Se o RF-17 **ainda não** estiver pronto quando este RF for implementado, crie um read gateway próprio (`IDashboardReadPersistenceGateway` + impl Prisma) com a mesma forma de `OrderInPeriod`, e a consolidação com o do RF-17 fica como nota no `progress-claude.txt`. **Confira o status do RF-17 antes de escolher.**
- **RF-01:** `Money`, `Period` (SP), `DomainError`/`toHttpResponse`, container + symbols, banco de teste + helpers.
- **RF-03:** `periodQuerySchema`; `src/lib/openapi.ts`.
- **Não altere `prisma/schema.prisma`** (criado só pelo RF-01).

## 4. Escopo — entregáveis (faça exatamente isto)

- `src/server/application/use-case/GetDashboardUseCase.ts` + `.../contracts/IGetDashboardUseCase.ts` — `period` → `DashboardData`. Lê os pedidos do período (read gateway — reusado do RF-17 ou próprio, ver §3), o total de ads (RF-16) e o custo fixo (RF-10); usa `PeriodReportCalculator.computePeriodProfit` (indicadores), `computeCostByCategory` (custo por categoria) e `computeMarginSeries` (granularidade `hour` para "today", `day` para períodos maiores). Serializa em `DashboardData` (centavos).
- **(Somente se o RF-17 não estiver pronto)** `src/server/application/gateway/IDashboardReadPersistenceGateway.ts` + `src/server/infrastructure/gateway/DashboardReadPrismaPersistenceGateway.ts` — read model `OrderInPeriod` (venda, frete, `itemsCost`, itens com `categoryName`/`cost` congelados). Caso o RF-17 já esteja `DONE`, **reuse `IReportPersistenceGateway`** e **não** crie esta porta.
- `src/app/api/dashboard/route.ts` — `GET` (period query) → `DashboardData`. Auth (Clerk) + `toHttpResponse` no `catch`.
- `src/lib/schemas.ts` — reusar `periodQuerySchema` (DTO de período, mesma convenção dos demais relatórios).
- `src/lib/openapi.ts` — registrar `GET /api/dashboard` (resposta `DashboardData`).
- `src/server/di/container.ts` + `symbols.ts` — binding de `GetDashboardUseCase` (+ read gateway próprio, se criado).

### Fora do escopo (NÃO faça)

- Não implementar `PeriodReportCalculator` nem o ad spend — vêm do RF-16 (só **consumir**).
- Não implementar `GetHistoryUseCase`/`ExportHistoryUseCase` nem `/api/history*` — RF-17.
- Não tocar o front (gráficos/indicadores do Dashboard) — RF-19.
- Não alterar `prisma/schema.prisma`.

## 5. Fim — Definition of Done

- `GET /api/dashboard?period=...` devolve `DashboardData`: `lucro = receita − custo − frete − ads − (custo fixo × nº pedidos)`; `custo por categoria` agrupado pela categoria **congelada** dos itens; `série de margem` por hora (dia) / dia (períodos maiores); `adsAvailable` refletindo a disponibilidade do TikTok (RF-16).
- O cálculo é delegado ao `PeriodReportCalculator` (RF-16); o use case **não** reimplementa as fórmulas.
- O read gateway é **reusado** do RF-17 quando este já existe (sem duplicar a leitura); caso contrário, criado com a mesma forma e documentado para consolidação.
- DTO de período e OpenAPI (§15) atualizados.
- **Testes da §14.2 passando** (`bun test`). `bun run lint` limpo; sem `any`.

## 6. Testes que cobrem este RF (§14.2)

Integração (`test/application/*.test.ts`, banco de teste + fakes externos):

- **`GetDashboardUseCase`** — **lucro = receita − custo − frete − ads − (custo fixo × nº pedidos)**; **custo por categoria** agrupado pela categoria **congelada** dos itens; série de margem por hora (dia) / dia (períodos maiores).

## 7. Trecho da PRD que fala deste RF

> ### F4 — Dashboard
>
> **História:** _Como dono, quero ver a performance do negócio por período, para entender lucro, margem e onde está o custo._
>
> **Requisitos:**
>
> - Seletor de período (hoje / semana / mês / personalizado).
> - Indicadores: **receita, custo, lucro, margem média, nº de kits**.
> - **Gasto com Ads do período** (vindo do TikTok — ver F5) descontado no lucro.
> - **Custo fixo por pedido** (das Configurações — F7) descontado no lucro.
> - **Gráfico de margem ao longo do tempo** (por hora no dia; por dia em períodos maiores).
> - **Gráfico de custo por categoria** (exige o detalhamento dos itens dos empacotamentos — F2/RN-2.8).
>
> **Regras de negócio:**
>
> - **RN-4.1** Os indicadores usam as mesmas fórmulas de F3: `lucro = receita − custo dos itens − frete − total de ads − (custo fixo × nº de pedidos)`; `margem média = (lucro ÷ receita) × 100`.
> - **RN-4.2 (custo por categoria):** soma, no período, o custo dos itens de cada empacotamento **agrupado pela categoria congelada** de cada item.
> - **RN-4.3** O período "personalizado" aceita data inicial e final escolhidas pelo usuário.
>
> **Critérios de aceite:**
>
> - Selecionei "esta semana" → vejo receita, custo, lucro (com Ads da semana), margem média e nº de kits, além dos dois gráficos.
> - O gráfico de custo por categoria mostra, por exemplo, Canetas R$X, Cadernos R$Y, somando o custo real das caixas da semana.

## 8. Trecho da TECH_SPEC que fala deste RF

Inventário de casos de uso (§6.2):

> | `GetDashboardUseCase` (DTO)     | period → indicadores + série de margem + custo por categoria               | (validação de período)                                                                |

Domain Service — métodos consumidos (§5.3):

> ```ts
> export class PeriodReportCalculator {
>     // lucro do período = receita − Σcusto − Σfrete − totalAds − (custoFixo × nºpedidos)  (RN-3.3)
>     computePeriodProfit(orders, totalAds: Money, fixedCost: Money): { revenue; cost; profit: Money; avgMarginPct: number };
>     // custo por categoria (RN-4.2)
>     computeCostByCategory(orders): { categoryName: string; cost: Money }[];
>     // série de margem ao longo do tempo (RN-4.1) — por hora (dia) / por dia (períodos maiores)
>     computeMarginSeries(orders, granularity: "hour" | "day"): { label: string; marginPct: number }[];
> }
> ```

Endpoint (§8):

> | GET    | `/api/dashboard`                     | Indicadores + série de margem + custo por categoria             | `periodQuery`                                   | `DashboardData`                           |

DTO de leitura — `DashboardData` (§8):

> ```ts
> type DashboardData = {
>     revenueCents: number; costCents: number; adsCents: number; fixedTotalCents: number;
>     profitCents: number; avgMarginPct: number; orderCount: number;
>     marginSeries: { label: string; marginPct: number }[];
>     costByCategory: { categoryName: string; costCents: number }[];
>     adsAvailable: boolean;
> };
> ```

Linha do catálogo (§18.2):

> | **RF-18** | Dashboard: `GetDashboardUseCase` (indicadores + custo por categoria + série de margem) + rota `/api/dashboard`. | RF-14, RF-16 | empacotamentos + calculator | endpoint `/api/dashboard` correto. **Testes:** integração `GetDashboardUseCase` (§14.2). |

## 9. Referências

- PRD: [`../PRD.md`](../PRD.md) — F4 completa (RN-4.1/4.2/4.3).
- TECH_SPEC: [`../TECH_SPEC.md`](../TECH_SPEC.md) — §6.2, §8 (`/api/dashboard`, `DashboardData`), §5.3 (`computeCostByCategory`/`computeMarginSeries`/`computePeriodProfit`), §7 (`IReportPersistenceGateway`, reúso).
- RF-17 (`./RF-17.md`) — read gateway `IReportPersistenceGateway` a reusar quando pronto.
- [`../../CLAUDE.md`](../../CLAUDE.md) — PersistenceGateway, padrão de use case, DIP.
