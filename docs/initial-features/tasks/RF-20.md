# RF-20 — Adapter real do TikTok (Pedidos + Ads)

> **Subtask de implementação.** Autocontido: este arquivo traz contexto, escopo fechado, início (pré-condição), fim (Definition of Done), testes que o cobrem e os trechos exatos da PRD e da TECH_SPEC. Implemente lendo **só este arquivo** (+ as seções referenciadas da PRD/TECH_SPEC) e siga `CLAUDE.md`/`AGENTS.md`.

| | |
| --- | --- |
| **Tipo** | back-end |
| **Onda** | **adiado** |
| **Depende de** | RF-12, RF-16 (devem estar `DONE` em `progress-claude.txt`) |
| **Habilita** | — (substitui os Fakes da integração TikTok em produção; não desbloqueia nenhum RF) |
| **Hotspots tocados** | `src/server/di/container.ts` e `src/server/di/symbols.ts` (troca de binding Fake→real por env). **Nenhum** outro hotspot: este RF **não** mexe em `src/lib/schemas.ts`, `src/lib/openapi.ts`, `src/lib/error-messages.ts`, casos de uso, entidades, rotas ou front. |

## 1. Objetivo (o que este RF entrega)

Pedidos e gasto com anúncios **reais** do TikTok entrando no espelho do nosso banco automaticamente (negócio). Implementação dos adapters HTTP reais (`TikTokOrdersHttpGateway`, `TikTokAdsHttpGateway`, base `TikTokHttpGateway`) — com OAuth/token, assinatura HMAC e webhook real verificado — **plugados atrás das mesmas portas/interfaces de RF-12/RF-16**, trocando os Fakes em produção sem tocar em nenhuma outra camada (técnico).

## 2. Contexto (por quê)

A loja vende ao vivo no TikTok; os pedidos (cliente, valor, frete, status) e o gasto com anúncios precisam vir **automaticamente** da plataforma — o operador não digita nada (PRD F5/RN-5.1/RN-5.2). Em RF-12/RF-16 toda a integração foi construída **atrás de interfaces (ACL) + espelho no nosso banco**, com **Fake adapters** ligando o produto fim-a-fim sem a API real (TECH_SPEC TS9/TS10). Este RF é o **único item adiado**: troca os Fakes pelos adapters reais. Foi adiado por um **risco aberto de acesso** (a API de Pedidos do TikTok no Brasil ainda não está confirmada e não há sandbox BR — TECH_SPEC §17), por isso só é construído após o gatilho externo abaixo.

## 3. Início — pré-condições (o que já precisa existir)

> **🚧 GATE / GATILHO EXTERNO (bloqueante):** este RF **só pode começar** quando o **acesso à TikTok Shop Partner API no Brasil estiver confirmado** no Partner Center (app aprovado, `app_key`/`app_secret`, loja autorizada com `shop_cipher`, e — para Ads — `app_id`/`secret`/`advertiser_id`). Enquanto o acesso BR **não** estiver confirmado, **não inicie** — o produto inteiro roda com os Fakes de RF-12/RF-16 e nada o bloqueia (TECH_SPEC §17, TS9). Confirmar também, no portal, os _lifetimes_ dos tokens do Shop (access ~7d / refresh ~1a **não confirmados** em página oficial; Ads token não expira) — ver §17.

Antes de começar, confira no repo (`progress-claude.txt` em `DONE`):

- **RF-12 `DONE`** — portas `application/gateway/ITikTokOrdersGateway.ts` e `ITikTokAdsGateway.ts`; ACL `domain/acl/TikTokOrderTranslator.ts` / `TikTokAdsTranslator.ts`; **Fakes** `test/fakes/FakeTikTokOrdersGateway.ts` / `FakeTikTokAdsGateway.ts`; entidade/repo `Order`; `IngestTikTokOrderUseCase`; `ListOrdersUseCase`; rota `GET /api/orders`; stub do webhook `POST /api/webhooks/tiktok/orders`.
- **RF-16 `DONE`** — `IAdSpendPersistenceGateway` + `GetAdSpendUseCase`/`SetManualAdSpendUseCase` (consomem `ITikTokAdsGateway`); `AdSpendDay(source=TIKTOK|MANUAL)`.
- **RF-02 `DONE`** (transitivo) — abstração `HttpClient` do back (`src/server/infrastructure/http/HttpClient.ts`) + `AxiosHttpClient` com o verbo `query` (QUERY). É a dependência dos HttpGateways.
- Símbolos `SYMBOLS.TikTokOrdersGateway` / `SYMBOLS.TikTokAdsGateway` / `SYMBOLS.HttpClient` já existentes em `src/server/di/symbols.ts`.

## 4. Escopo — entregáveis (faça exatamente isto)

Toda a implementação fica **atrás das portas já definidas** (`ITikTokOrdersGateway`/`ITikTokAdsGateway`) e usa a **ACL existente** (`TikTokOrderTranslator`/`TikTokAdsTranslator`) e a abstração **`HttpClient`** (nunca `axios`/`fetch` direto).

- **`src/server/infrastructure/gateway/TikTokHttpGateway.ts`** — classe **base** comum (`@injectable`), recebe `@inject(SYMBOLS.HttpClient)`. Concentra o que é comum à integração: injeção do `HttpClient`, montagem de headers via `setHeaders`, **assinatura HMAC-SHA256 (`sign`)** dos requests do Shop, base URLs/hosts e o tratamento de erro externo → `TIKTOK_API_ERROR`/`TIKTOK_NOT_CONFIGURED` (502) lançados **direto no gateway** (§9).
- **`src/server/infrastructure/gateway/TikTokOrdersHttpGateway.ts`** — **estende** `TikTokHttpGateway` e implementa `ITikTokOrdersGateway` (`searchOrders(period)` backfill via `POST /order/202309/orders/search`; `getOrder(id)` detalhe). Host `https://open-api.tiktokglobalshop.com`, versão `202309`. OAuth/token (`token/get`, `token/refresh`) e `shops` (`shop_cipher`). Tradução resposta→domínio pela **ACL** `TikTokOrderTranslator.toDomain()`.
- **`src/server/infrastructure/gateway/TikTokAdsHttpGateway.ts`** — **estende** `TikTokHttpGateway` e implementa `ITikTokAdsGateway` (`getSpend(period)`). Host `https://business-api.tiktok.com/open_api`, `v1.3`. OAuth (`oauth2/access_token`), `advertiser/get`, gasto via `report/integrated/get`. Tradução pela **ACL** `TikTokAdsTranslator`. Janela máx ~30 dias/request (fatiar/paginar).
- **OAuth / token handling** — obtenção e **refresh** do token do Shop (usar expiry absoluto retornado); token do Ads não expira. Guardar/recuperar credenciais conforme convenção do projeto (env/config) — **sem** criar schema/migração nova (não é deste RF).
- **HMAC signing** — assinatura HMAC-SHA256 dos requests do Shop (`app_key` + `timestamp` + path/params), header `x-tts-access-token`, `shop_cipher` na query.
- **Verificação HMAC real do webhook** — no handler `POST /api/webhooks/tiktok/orders` (que **já existe** desde RF-12 e chama `IngestTikTokOrderUseCase`): verificar HMAC-SHA256 de `app_key + rawBody` no header `Authorization` antes de processar `type=1 ORDER_STATUS_UPDATE`. **Implemente apenas a verificação de assinatura** (lógica de infra/segurança); **não** reescreva o use case nem a forma da rota.
- **Job de sync/backfill** — busca por período (primeiro carregamento / agendado) via `ITikTokOrdersGateway.searchOrders` → `IngestTikTokOrderUseCase`; e Ads via `ITikTokAdsGateway.getSpend` → grava `AdSpendDay(source=TIKTOK)`. Reaproveita os use cases já prontos.
- **DI (troca Fake→real por env):** em `src/server/di/container.ts`, ligar `SYMBOLS.TikTokOrdersGateway`/`SYMBOLS.TikTokAdsGateway` aos adapters **reais** quando o env indicar produção/credenciais presentes, mantendo o **Fake** como default em dev/teste. Edição **aditiva** (não reescrever bindings existentes).

### Fora do escopo (NÃO faça)

- **NÃO** alterar **nenhum** caso de uso, entidade, value object, domain service, ACL, rota (forma/contrato), schema Zod, OpenAPI ou dicionário de erros. As portas, a ACL, os use cases e a rota do webhook **já existem** (RF-12/RF-16) e ficam **intactos** — este RF só preenche a **implementação dos adapters** atrás delas.
- **NÃO** tocar em **nada** do front-end.
- **NÃO** mudar `prisma/schema.prisma` (RF-01 já contempla `Order`/`AdSpendDay`).
- **NÃO** trocar o Fake em dev/teste: os **testes de integração dos casos de uso continuam com o Fake** (§14) — ver §6.
- **NÃO** começar antes do gate da §3.

## 5. Fim — Definition of Done

- Pedidos e Ads **reais** entram pelas **mesmas interfaces** (`ITikTokOrdersGateway`/`ITikTokAdsGateway`): a lista de Pedidos (`/api/orders`) e o ad spend (`/api/ad-spend`) refletem dados reais quando o binding real está ativo, **sem** nenhuma alteração em use cases/rotas/front.
- `TikTokOrdersHttpGateway` e `TikTokAdsHttpGateway` **estendem** `TikTokHttpGateway` e **dependem da abstração `HttpClient`** (nunca `axios`/`fetch` direto), traduzindo via ACL.
- OAuth/refresh de token do Shop, assinatura HMAC dos requests e **verificação HMAC do webhook** funcionando; erros externos → `502` `TIKTOK_API_ERROR`/`TIKTOK_NOT_CONFIGURED` (§9).
- DI troca **Fake↔real por env**, Fake como default em dev/teste.
- Este RF **não muda** nenhum caso de uso, entidade, rota ou front (verificável no diff). `bun run lint` limpo; sem `any`.
- **Validação manual** do adapter real feita no sandbox/portal (Ads em `sandbox-ads.tiktok.com`; Pedidos não tem sandbox BR — validar no Partner Center) e registrada nas notas do `progress-claude.txt`.

## 6. Testes que cobrem este RF (§14.2)

- **Não há novos testes automatizados para o adapter real.** Implementações de infraestrutura (gateways HTTP, HttpClient, renderers) **não são alvo de teste** (§14.1) — entram só como apoio.
- Os **testes de integração dos casos de uso** que usam TikTok (`IngestTikTokOrderUseCase`, `ListOrdersUseCase`, `GetAdSpendUseCase`, `SetManualAdSpendUseCase`) **continuam rodando com o Fake** (`test/fakes/FakeTikTokOrdersGateway`/`FakeTikTokAdsGateway`) — não trocar para a API real nos testes.
- O **adapter real valida-se manualmente** no sandbox/portal do TikTok (estado explícito de aceite, não suíte automatizada).

## 7. Trecho da PRD que fala deste RF

> ### F5 — Integração com o TikTok (Pedidos + Ads)
>
> **História:** _Como dono, quero que os pedidos e o gasto com anúncios venham automaticamente do TikTok, para o operador não digitar cliente/valor/frete e o lucro refletir o custo real de mídia._
>
> **Requisitos:**
>
> - Conectar a conta do TikTok da loja (fluxo de conexão da integração).
> - Puxar, por período, **os pedidos** com: id do pedido, @cliente, valor da venda, frete, data/hora do pedido e **status de envio** (pendente/enviado). Alimenta a página Pedidos (F2) e o Histórico (F3).
> - Puxar, por período, o **total gasto com Ads**. Alimenta o CPA (F3) e o lucro (F3/F4).
> - **Fallback de Ads:** quando a integração de Ads está indisponível/sem dado, mostrar Ads como **indisponível** e **permitir um valor manual** de anúncios para o período, para não travar a análise.
>
> **Regras de negócio:**
>
> - **RN-5.1 (pedidos automáticos):** cliente, valor da venda, frete e status vêm do TikTok; o operador **não digita** nem cria pedidos manualmente.
> - **RN-5.2 (Ads automático):** havendo integração e dado, o Ads do período é **o valor do TikTok**.
> - **RN-5.3 (fallback de Ads):** sem dado/conexão de Ads, o operador pode informar um **valor manual**, usado no cálculo de lucro/CPA daquele período. _(Os pedidos não têm fallback manual — sem integração, não há pedidos a empacotar.)_
> - **RN-5.4 (uso dos números):** Ads/CPA e custo fixo entram **somente** nos agregados (F3/F4), **nunca** na margem ao vivo do empacotamento (RN-2.5).
> - **RN-5.5 (resiliência):** falha da integração **nunca** quebra as telas — o que já foi sincronizado continua visível, com aviso amigável.
>
> **Critérios de aceite:**
>
> - Com a conta conectada, a página Pedidos de "hoje" lista os pedidos do TikTok, já com cliente/valor/frete preenchidos.
> - O dashboard de "hoje" mostra o gasto com Ads automático e o lucro já descontando ads e custo fixo.
> - Sem dado de Ads, o dashboard mostra "Ads indisponível" e um campo manual; ao digitar R$60, lucro e CPA recalculam.
>
> > **Pendências para a tech spec (F5):** qual(is) API(s) do TikTok (TikTok Shop para pedidos, TikTok Ads para gasto), como autenticar/autorizar, qual conta/loja/anunciante, mapeamento de período/fuso (São Paulo), frequência de sincronização e cache, modelo de status de envio, e granularidade do valor manual de _fallback_ de Ads.

Da **§8 — Pendências para a tech spec** (PRD):

> - Integração TikTok: **API de pedidos** (TikTok Shop) e **API de Ads** são produtos distintos — auth, contas/loja/anunciante, sincronização (push/webhook vs _polling_), modelo de status de envio, mapeamento de período/fuso (São Paulo) e cache.

## 8. Trecho da TECH_SPEC que fala deste RF

Da **§1 — Decisões técnicas**:

> | TS9  | **TikTok atrás de interface (ACL) + espelho no nosso banco.** Tudo é construído com um **Fake adapter**; o **adapter real do TikTok + webhook é o único item adiado** até confirmar acesso à API BR. |
> | TS10 | Sincronização de pedidos: **webhook `ORDER_STATUS_UPDATE` + backfill via `orders/search`**, espelhado no nosso banco. O resto do app só lê do nosso banco. |
> | TS11 | Identificação do pedido: **número do pedido (TikTok) + nome do destinatário** (sem máscara se a API permitir; com máscara se for o que vier).          |

Da **§7 — Repositórios e gateways**:

> - **External Gateway (HttpGateway)** = integração externa por HTTP. Contrato em `application/gateway/`; implementação em `infrastructure/gateway/` com sufixo **`HttpGateway`** (ex.: `TikTokHttpGateway`); **depende da abstração `HttpClient`** (nunca de `axios`/`fetch` direto) e usa **ACL** (domínio) para traduzir. Detalhe do `HttpClient` em §7.3.

> | External Gateway    | `application/gateway/ITikTokOrdersGateway`              | `infrastructure/gateway/TikTokOrdersHttpGateway` (fake: `test/fakes/FakeTikTokOrdersGateway`) |
> | External Gateway    | `application/gateway/ITikTokAdsGateway`                 | `infrastructure/gateway/TikTokAdsHttpGateway` (fake: `test/fakes/FakeTikTokAdsGateway`) |

> > **Nomeação TikTok:** os dois HttpGateways (Pedidos via Shop API e Ads via Business API — hosts/auth distintos) estendem uma base comum **`TikTokHttpGateway`** que concentra `HttpClient` + assinatura/headers; `TikTokOrdersHttpGateway` e `TikTokAdsHttpGateway` herdam dela. Assim respeitamos a convenção `<Integração>HttpGateway` e o ISP das portas.

Da **§7.3 — `HttpClient` (back-end)** — uso tipado (QUERY/POST):

> **Exemplo de uso tipado (HttpGateway do back):** body e headers próprios da integração, ambos checados pelo compilador.
>
> ```ts
> type TikTokAuthHeaders = { "x-tts-access-token": string; "x-tts-sign": string };
> type SearchOrdersBody = { create_time_ge: number; create_time_lt: number; page_size: number };
>
> const data = await this.http.query<TikTokOrdersResponse, SearchOrdersBody, TikTokAuthHeaders>(
>     "/order/202309/orders/search",
>     { create_time_ge, create_time_lt, page_size: 100 },
>     { headers: { "x-tts-access-token": token, "x-tts-sign": sign } },
> );
> ```
>
> - `axios.request({ method: "QUERY" })` aceita métodos customizados; usar QUERY só onde a integração suportar (caso contrário, `post`). Suporte a QUERY ainda é emergente — ver §17.
> - DI: `HttpClient` é um símbolo no container do back; `TikTokHttpGateway` recebe `@inject(SYMBOLS.HttpClient)`.
> - `bun add axios`.

Da **§10 — Integração TikTok (Pedidos + Ads)**:

> > **TS9/TS10:** tudo construído atrás de interfaces (ACL). O **adapter real + webhook** é o único item **adiado** até confirmar acesso à Partner API BR. Em dev/homologação usamos **Fake adapters**.
>
> ### 10.1 Portas (interfaces)
>
> ```ts
> // application/gateway/ITikTokOrdersGateway.ts
> export interface ITikTokOrdersGateway {
>     searchOrders(period: Period): Promise<TikTokOrderDTO[]>;   // backfill
>     getOrder(tiktokOrderId: string): Promise<TikTokOrderDTO>;  // detalhe (se preciso)
> }
> // application/gateway/ITikTokAdsGateway.ts
> export interface ITikTokAdsGateway {
>     getSpend(period: Period): Promise<{ amountCents: number } | { unavailable: true }>;
> }
> ```
>
> **Implementações (HttpGateway):** `TikTokOrdersHttpGateway` e `TikTokAdsHttpGateway` estendem a base **`TikTokHttpGateway`** e dependem da abstração **`HttpClient`** (§7.3) — nunca de `axios` direto. A base concentra o que é comum à integração (injeção do `HttpClient`, montagem de headers via `setHeaders`, assinatura HMAC dos requests do Shop, base URLs). A tradução resposta↔domínio é feita pela **ACL** (`TikTokOrderTranslator` / `TikTokAdsTranslator`). Os **Fakes** (`FakeTikTokOrdersGateway`/`FakeTikTokAdsGateway`) implementam as mesmas portas sem `HttpClient` (dados em memória).
>
> ### 10.2 Endpoints reais (pesquisa) — para o adapter real
>
> **Pedidos — TikTok Shop Partner API** (host `https://open-api.tiktokglobalshop.com`, versão `202309`):
>
> | Passo | Método | Caminho | Notas |
> | ----- | ------ | ------- | ----- |
> | Token | GET | `auth.tiktok-shops.com/api/v2/token/get` | `app_key`, `app_secret`, `auth_code`; usa expiry absoluto retornado |
> | Refresh | GET | `auth.tiktok-shops.com/api/v2/token/refresh` | `refresh_token` |
> | Shops | GET | `/authorization/202309/shops` | obtém `shop_cipher` (necessário nas chamadas) |
> | Buscar pedidos | **POST** | `/order/202309/orders/search` | filtros de tempo (`create_time_ge/lt`), `page_size≤100`, `page_token`; status |
> | Detalhe | GET | `/order/202309/orders?ids=...` | só se faltar campo no summary |
>
> - **Auth por request:** header `x-tts-access-token`, `shop_cipher` na query, `app_key`+`timestamp`, e **assinatura HMAC-SHA256** (`sign`).
> - **Campos:** `payment.total_amount` (venda), `payment.shipping_fee` (frete), `create_time` (data), `order_status`. **Sem @username** → usar `recipient_address` (nome, possivelmente mascarado) + número do pedido (TS11).
> - **Status → nosso `ShipmentStatus`:** `UNPAID|AWAITING_SHIPMENT|AWAITING_COLLECTION|PARTIALLY_SHIPPING` → `PENDING`; `IN_TRANSIT|DELIVERED|COMPLETED` → `SHIPPED`; `CANCELLED` → `CANCELLED`.
> - **Webhook:** `POST /api/webhooks/tiktok/orders` recebe `type=1 ORDER_STATUS_UPDATE`; verificar HMAC-SHA256 de `app_key + rawBody` no header `Authorization`; chamar `IngestTikTokOrderUseCase`.
>
> **Ads — TikTok Business/Marketing API** (host `https://business-api.tiktok.com/open_api`, `v1.3`):
>
> | Passo | Método | Caminho | Notas |
> | ----- | ------ | ------- | ----- |
> | Token | POST | `/v1.3/oauth2/access_token/` | `{app_id, secret, auth_code}` — **token não expira** |
> | Advertiser | GET | `/v1.3/oauth2/advertiser/get/` | obtém `advertiser_id` |
> | Gasto | GET | `/v1.3/report/integrated/get/` | `report_type=BASIC`, `data_level=AUCTION_ADVERTISER`, `dimensions=["advertiser_id"]`, `metrics=["spend"]`, `start_date`, `end_date` |
>
> - **Total do período:** sem dimensão de tempo, vem 1 linha agregada `data.list[0].metrics.spend`. Janela máx ~30 dias/request (paginar/fatiar).
> - **Sandbox Ads:** host `https://sandbox-ads.tiktok.com/open_api` (baixo risco). **Sandbox de Pedidos:** só UK/Indonésia (não há BR) — risco §17.
>
> ### 10.3 Espelho + sincronização
>
> - `IngestTikTokOrderUseCase` usa `TikTokOrderTranslator.toDomain()` → `OrderRepository.upsert()`. O webhook e o backfill (busca por período no primeiro carregamento / agendado) alimentam o espelho.
> - Ads: um job (ou no `GetAdSpendUseCase`) busca via `ITikTokAdsGateway`, grava `AdSpendDay(source=TIKTOK)`. Se `unavailable`, o front usa o valor `MANUAL` do dia (se houver) e marca `available:false`.
>
> ### 10.4 Fake adapter (dev/testes)
>
> `FakeTikTokOrdersGateway` e `FakeTikTokAdsGateway` retornam dados realistas (derivados do seed atual do front). É o que liga o produto fim-a-fim sem a API real, e o que os testes de integração de casos de uso usam.

Da **§16 — Ordem de construção**:

> 7. **Adapter real do TikTok + webhook:** **adiado** até confirmar acesso à Partner API BR (plugar atrás das mesmas interfaces, sem tocar no resto).

Da **§17 — Riscos e pendências**:

> - **🔴 Acesso à API de Pedidos do TikTok no Brasil não confirmado** e **sem sandbox BR** (só UK/Indonésia). Mitigação: tudo atrás da interface + Fake (TS9); validar acesso no Partner Center antes do milestone 7. **Bloqueia só o adapter real**, não o produto.
> - **Identificação do cliente:** TikTok não dá @username; usamos número do pedido + nome do destinatário (possivelmente mascarado). Confirmar na resposta real se o nome vem completo.
> - **Tokens TikTok:** lifetimes do Shop (access ~7d / refresh ~1a) **não confirmados** em página oficial legível — validar no portal. Ads token não expira (confirmado).

Da **§18.2 — Catálogo de RFs** (linha-espinha deste RF):

> | **RF-20** | **Adapter real TikTok** (adiado): `TikTokOrdersHttpGateway` + `TikTokAdsHttpGateway` (base `TikTokHttpGateway`), OAuth, assinatura HMAC, webhook real, sync/backfill. Substitui os Fakes atrás das mesmas portas. | RF-12, RF-16 | **acesso à API BR confirmado** | pedidos/ads reais entram pelas mesmas interfaces. **Testes:** integração dos use cases continua com Fake; o adapter real valida-se manualmente no sandbox/portal. |

## 9. Referências

- PRD: [`../PRD.md`](../PRD.md) — F5, §7 (D8/D14/D16/D17), §8 (pendências TikTok).
- TECH_SPEC: [`../TECH_SPEC.md`](../TECH_SPEC.md) — §1 (TS9/TS10/TS11), §7 + §7.3, §10 (10.1–10.4), §16 (milestone 7), §17 (riscos), §18.2 (RF-20).
- [`../../CLAUDE.md`](../../CLAUDE.md) — Repository vs PersistenceGateway, External Gateway/ACL, DIP, modelo de erros.
- [`./README.md`](./README.md) — DoD geral, hotspots, ordem de execução (adiados).
- [IETF `draft-ietf-httpbis-safe-method-w-body`](https://datatracker.ietf.org/doc/draft-ietf-httpbis-safe-method-w-body/) — verbo QUERY (usado no `HttpClient`).
- Guias do Next em `node_modules/next/dist/docs/` (este projeto é um Next 16 com breaking changes) — caso a rota do webhook precise de ajuste de leitura do `rawBody`.
